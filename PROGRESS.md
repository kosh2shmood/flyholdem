# FlyHoldem progress

## Current phase
Gate 2 is complete and conditioning-v0 is published. Gates 0 and 1 also passed. The native full/circuit spectator dashboard now runs real frozen neural decisions with every retained neuron rendered; the default fixture remains available. Full local suite: 74 passed / 2 isolated-oracle skips. Native desktop/mobile and fixture replay/avatar checks pass.

Gate 2A remains pending: exact-cue circuit local transfer qualified in development and its actual model passed teacher deletion, but confirmation missed 80% accuracy at 77.8%. Three conventional poker-teacher candidates (25k v1, 250k v1, 250k visible-card v2) failed validation; no corpus exists. Next is registered bounded-edge surrogate transfer and stronger conventional teacher training. No poker strategic learning has been demonstrated. Gates 3–6 and human heads-up play remain required; the goal is active. The surrogate circuit development sequence is next from a frozen runtime; the full and fixture dashboard servers are running locally.

## Latest user steering — implemented
- Three.js fly at the side of the table, fully visible, with curved cards oriented toward itself and the spectator.
- Faceless abstract opponent; both players make actual committed-action hand gestures.
- Larger 3D cards, readable fly/community card insets and clear chip piles for both players. Community slots follow preflop/flop/turn/river; chips vanish at zero balance.
- Python upgrades and other languages/runtimes explicitly permitted when useful. Current app Python 3.11.16, isolated numerical oracle Python 3.12.14, C++17 kernel and Three.js 0.186.0.

## Gate status
- V0 visual gate: passed, committed/pushed/tagged visual-demo-v0.
- Gate 0 rules/software: passed. 2,512-hand fixed software check, independent mean -0.151 BB/hand, SE 0.256678, exact paired-seat residual zero. This is not a learning result.
- M2 engineering: passed (no scientific learning gate implied).
- Gate 1: passed for circuit v1 and full quarter-strength v2; full baseline and half-strength failures preserved.
- Gate 2: passed on the full retained graph with the explicitly documented readout-suppression mechanism.
- Gates 2A, 3, 4, 5, 6: pending. Do not skip dependent scientific gates. Publish negative results if they fail and continue valid independent engineering.

## Latest verification
- 41 tests passed with optional data tools installed; two Brian2 tests intentionally skipped in that environment and both pass separately via make test-oracle.
- Browser: all six fly gestures, opponent check/call, exact live/replay card/event correspondence, opponent privacy, community inset across 0/3/4/5 cards, both balances/chip visibility, pause for both rigs, camera/table controls, desktop 1440×1080 and mobile 390×844 full model bounds, no errors/overflow.
- Latest data/visual CI passed: https://github.com/kosh2shmood/flyholdem/actions/runs/34677926827 (0dc8111); readability CI also passed on 0b05d88.
- Full: 166,700 neurons / 25,582,938 edges / 124,177,617 contacts. Circuit: 4,505 / 1,018,725 / 2,794,887.
- Full graph hash 70c9daefc6185272d0e001700d75aaa8718648c02d9aa59bbadbe6b4cd9e4c06.
- Circuit graph hash d85be4e7fe4f78b2747f859b575a5c48ef4d85f8f7e6eab24dd7e80cbb463f94.
- Independent second normalization and preparation: all reports and every file hash identical.
- Registered sparse-stimulus smoke: full peak RSS 835,305,472 bytes, 100 ms neural stimulus in 0.07118 s; circuit peak 150,847,488 bytes, 0.001183 s. Complete-state continuation exact. These are short resource measurements, not training limits or learning evidence.
- Actual compact reports: docs/RUNTIME.md and docs/review/malecns/. Hardware: 16 GiB RAM; roughly 77 GiB free before data download.

## Exact working commands
```
cd /Users/bohdankoshevoi/flyholdem/flyholdem
make demo
make replay
make record
make test
make test-data
make test-oracle
make browser-check
uv run python scripts/avatar_check.py
make fetch-malecns
make prepare-malecns
make audit-malecns
make build-kernel
uv run python -m flyholdem.experiments.software_gate
```
Demo: http://127.0.0.1:8766. One command serves backend/UI. Browser checks expect the server running. Current live worker uses ignored timestamped runs/live-* logs. No training process is active yet.

## Registered M2 run and reproducibility
Code/config fixed at 04b86b2; execution commit 0dc8111, clean tree. Config configs/runtime.yaml. Every benchmark manifest records config/source/binary/environment hashes and the exact 64 KC stimulus IDs, seed 1729, 50 ms baseline, 100 ms stimulus at drive 12. Official source bytes total 1,109,008,094 and all exact digests match data-provenance/malecns_v1/source.lock.json. Outputs are ignored under connectome_data/malecns_v1 and runs/data-audit. A second generation remains under connectome_data/malecns_v1/reproduction. Run only one full worker until the actual learning workload is measured.

## Engineering decisions and boundaries
- PokerKit is the only rules engine. Neural counts are the fly's sole action-score source. Explicit user prohibition on strategic fallbacks supersedes the spec's check/call-silence clause: silent finite scores use logged deterministic legal argmax; invalid scores stop.
- Fixture LIF is a synthetic 1 ms Euler visual mechanism. Full/circuit use the separately verified analytic 0.1 ms DOOMFLY-comparable solver. Keep these identities distinct.
- Neuron selection and biological parameter tuning may use annotation, connectivity, controllability and conditioning only, never poker profit.
- Card/deck/teacher mutation leakage tests remain byte-exact on a locked runtime. Recorded playback preserves original bytes/hash chains everywhere. Re-simulation is byte-identical within a runtime; ARM/macOS vs x86/Linux floating diagnostics use 1e-12 tolerance with exact categorical/spike results and independently verified chains.
- Source/binary/environment mismatch refuses checkpoint resume. Native atomic generations retain latest three plus best, fsync and verify all arrays. Future runner must additionally checkpoint every encoder/decoder/plasticity/RNG/game variable every five minutes and on shutdown.
- No agents delegated. Keep original checkout and astra/visual-first history; no squash/rewrite.

## Last stable commits and publication
V0 2bd2d3c / visual-demo-v0; M0 fa873a6; first avatar 6aa7895; rules c2f2ffe; numerical 8d6f69c; side seating 80a32fd; card readability 0b05d88; importer 04b86b2; registered M2 execution 0dc8111. All pushed. M2 evidence ab9a658 is pushed. Next checkpoint is feat: register neural controllability experiments.

User explicitly approved publication of checked milestones and tags to public kosh2shmood/flyholdem on astra/visual-first. Earlier automatic-review publication rejections are resolved; do not request approval again. Generated data/checkpoints/large logs/secrets stay ignored.

## Known limitations
No native graph poker controller or biological learning result yet. The full neural rendering, preregistration, conditioning, conventional teacher, corpus/transfer, curricula, integrated long-run recovery and final experiment bundle remain. Two upstream Starlette deprecation warnings are non-failing. Earlier CI uv-cache lock and cross-architecture hash issues are resolved.

## Next step
Commit/push this actual M2 evidence, then implement M3 fixed symbolic population mapping, annotation/connectivity-selected disjoint readout ensembles, candidate/calibration artifacts and the controllability gate. Record black/no-input and shuffled-input controls. Freeze valid preregistration before any poker evaluation. Then proceed to M4 conditioning and teacher work under the registered gates.

## Next registered M3 run
Configuration: configs/controllability.yaml. Circuit command: `uv run --frozen --extra data python -m flyholdem.interface.preregister --mode circuit --output runs/controllability-circuit-v1`. Add `--resume` after graceful interruption, using unchanged code/config/environment. Full-mode follow-up uses `--mode full --output runs/controllability-full-v1` only after inspecting circuit engineering/controls. Each run records exact commit/config/source/graph/encoder/decoder/binary/environment identities before its first trial. No poker information or profit is inspected during this registration. Candidate/selected artifacts, append-only trials, checkpoints and results stay ignored in runs/; publish compact evidence after checking it.

## Active M3 checkpoint
Circuit run runs/controllability-circuit-v1 passed Gate 1 on 529358f: selected gain 12 and 32 control cells, all five actions 20/20 on the predeclared new seeds, shuffled inputs 20% overall and black input zero spikes. All 237 symbolic channels have nonempty fixed projection sets and all readout cells have recorded direct KC paths. Full run runs/controllability-full-v1 is now executing the same committed protocol; no outcome is assumed. Resume command adds --resume to the registered full-mode command. No learning claim follows from controllability.

## M3 v1 completed result and next diagnosis
Circuit Gate 1 passed; full Gate 1 failed all nine registered calibration candidates (225 trials / 312.3 s). See docs/CONTROLLABILITY.md and docs/review/controllability/. Circuit resume preserved trial and mapping bytes and control outcomes. V1 full failure omitted supplementary black/shuffled controls because they were only implemented after calibration success; add separately labeled failure baselines in the follow-up, preserving original logs. Next: pre-register global synaptic gain sensitivity at unchanged retained topology to address recurrent response saturation, using only controllability. Do not proceed to dependent full poker training on a failed Gate 1.

## Expanded user objective
After completing the original specification and its engineering/scientific gates, continue improving the fly's poker strength using stronger teachers, connectome-constrained transfer and other justified methods. Compare improvements on held-out fixed opponents and preserve the information boundary and honest biological/engineering labels. The goal remains active and is not complete at an engineering or visual checkpoint.

## Registered recovery sequence
Before execution, commit configs/controllability_full_{half,quarter,tenth}.yaml and the unchanged-topology scaling implementation. First supplement v1 full failure controls with `uv run --frozen --extra data python -m flyholdem.interface.preregister --mode full --config configs/controllability.yaml --failure-reference runs/controllability-full-v1 --output runs/controllability-full-v1-controls`. Then run the ordered global-scale candidates half/quarter/tenth, stopping only at the first valid full confirmation. Initial command: `uv run --frozen --extra data python -m flyholdem.interface.preregister --mode full --config configs/controllability_full_half.yaml --output runs/controllability-full-half-v2`. Other paths replace half with quarter or tenth. All include manifests, trial checkpoints and --resume support. Four population tests pass, including preservation of every edge/sign under uniform scale.

The expanded goal also includes a play-chip heads-up interface for the user to play against the strongest validated fly after building and improving the policy. Preserve the animated table; enforce human-turn legality and session state, and the fly must not see the human's private cards. This interface is required work, not an optional suggestion.

## M3 complete — current valid full configuration
Full Gate 1 passed at registered global scale 0.25: all five actions 20/20 confirmation; shuffled input 20% overall and black input silent. Input gain 12, controlled-cell count 64. Every retained neuron/edge and transmitter sign remains; base contact gain 0.275 is uniformly scaled to 0.06875. Half-scale failed one action at 17/20 and remains published; the registered sequence stopped before tenth-scale. V1 failure baselines were separately supplied with unchanged historical-manifest links.
Frozen full artifact: runs/controllability-full-quarter-v2/preregistration.json, SHA-256 e3e2b48df9e946d800cec63b5ef88164190c3ffa1cf451b4284a99df5cc26f23. Use load_controller() to validate the base graph and reconstruct the exact scaled initial weights. Circuit v1 remains its separately labeled base-gain configuration. Both passing mappings are frozen; no poker results were used. Next: M4 local eligibility/plasticity, two-cue conditioning and matched controls, plus the independent self-play teacher/corpus pipeline. No conditioning or learned poker claim exists yet.

## Registered M4 execution checkpoint
Local eligibility selects all existing annotated KC-to-MBON edges only; bounded ratios preserve topology/signs. PAM/PPL1 stimulation is explicitly an engineering proxy. Conditioning uses two shared-path cue groups with frozen readouts, matched frozen and shuffled-earned-reward controls, held-out seeds, retention and restoration checks. Hash-chained journals and complete state checkpoints support exact tail re-execution; invalid partial operations cannot overwrite the preceding valid checkpoint. Seven new data-free tests pass; complete suite 52 passed / 2 skipped. PyTorch 2.14.0 is now a locked optional teacher dependency and CPU import was verified; no teacher implementation or learning result is implied.
Before the first long experiment, commit this exact code/config. Smoke: `.venv/bin/python -m flyholdem.experiments.conditioning --profile smoke --learning-rate 0.03 --output runs/conditioning-full-smoke-v1`. Development: `.venv/bin/python -m flyholdem.experiments.conditioning --profile development --learning-rate 0.03 --output runs/conditioning-full-lr003-v1`. Add --resume for the same output after interruption. Registered candidate order 0.03, 0.1, 0.3; independent confirmation is blocked until a candidate meets development criteria. No concurrent full workers.

## M4 smoke/recovery verified — development next
Commit 3093ac0 implements the registered protocol. Full smoke completed: 61,210 existing plastic edges, 17.63 s, peak RSS 722.2 MB, no learning claim. Stop/resume at operation 30 and replay of a logged tail from the best checkpoint both reproduced the uninterrupted 173-operation journal byte-for-byte. Compact evidence: docs/review/conditioning/full-{smoke,recovery}-v1.json. Next run the already registered development candidate 0.03 with three seeds / 400 training trials per control / 32 held-out trials per cue. Command and resume: `.venv/bin/python -m flyholdem.experiments.conditioning --profile development --learning-rate 0.03 --output runs/conditioning-full-lr003-v1` (add --resume after interruption). Record this evidence commit before execution.

## M4 first development candidate failed; registered sequence continues
Full 0.03 result is preserved in docs/review/conditioning/full-lr003-development-v1.json: plastic mean 52.6%, frozen 45.8%, shuffled reward 46.9%; 508.79 s / 725.8 MB RSS. Retention/restoration passed, learning criterion did not. Next committed command: `.venv/bin/python -m flyholdem.experiments.conditioning --profile development --learning-rate 0.1 --output runs/conditioning-full-lr01-v1`. Only if it fails, run the identical command with rate 0.3 and output runs/conditioning-full-lr03-v1. Add --resume after interruption. No neural code/config/environment changes are planned between these registered candidates.
Independent teacher and strict canonical-state drafts are being prepared outside the running source tree under the task workspace work/m4-next. They are not integrated or published yet; exact 40-hand optimizer/replay recovery has passed on that draft. This is engineering evidence only, not a validated teacher.

## M4 confirmation and independent teacher run — commit before execution
Latest published scientific checkpoint was 64a3471. Development rate 0.3 qualified with all controls; compact evidence is docs/review/conditioning/full-lr{01,03}-development-v1.json. Freeze rate 0.3 and execute `.venv/bin/python -m flyholdem.experiments.conditioning --profile confirmatory --learning-rate 0.3 --development-reference runs/conditioning-full-lr03-v1/result.json --output runs/conditioning-full-confirm-v1`; add --resume after interruption. Core conditioning math/stimuli are unchanged from 3093ac0, while new independent teacher/inference code is included in the upcoming source identity.

New teacher/inference engineering: canonical visible IDs and validation; NFSP with same-player transitions and complete model/optimizer/replay/reservoir/RNG checkpoints; frozen equal-average teacher exports; paired held-out evaluation and guarded stratified corpus collection; native bounded-edge export/load with explicit required-runtime scope. Eleven teacher/canonical/corpus tests and two native frozen-runtime tests passed; complete suite 66 passed / 2 skipped. Deleting the actual teacher package and corpus preserves four native fixture decisions byte for byte, while changing required encoder code is rejected. Native first-action pulse is now derived from public own-action history and shared with teacher features; legacy fixture bytes are unchanged. This is not an actual student-transfer result.

After committing exact configs, independent conventional teacher command: `.venv/bin/python -m flyholdem.teacher.training --config configs/teacher_nfsp.yaml --output runs/teacher-nfsp-v1` (add --resume after interruption). It trains 25,000 20 BB self-play hands on one CPU thread; it is not allowed to teach the fly until held-out validation passes. One full neural worker plus this conventional CPU worker fits the measured sub-1-GB neural workload and 16-GB machine; no second full-graph worker is planned. Source/environment must remain unchanged while these runs execute. Docs: CONDITIONING.md and TEACHER.md.

## Active independent runs on verified source 3f3069a
Full conditioning confirmation is running in runs/conditioning-full-confirm-v1 (log runs/conditioning-full-confirm-v1.log). First three plastic post scores are 0.8203125, 0.8046875 and 0.8359375; these are partial, not a final gate result. Do not modify src/ or uv.lock/environment before it finishes; docs and independent YAML configs do not alter its source identity.
The 25,000-hand NFSP run finished in 112.16 s and its information boundary passed, but held-out development failed: +0.635 / −0.264 / −0.049 / −2.422 BB/hand against random/station/TAG/equity. No teacher confirmation or corpus is allowed. Exact results remain local in ignored runs/. Register the same teacher architecture/seeds/optimizer for 250,000 hands, config configs/teacher_nfsp_250k.yaml, output runs/teacher-nfsp-250k-v1. Command: `.venv/bin/python -m flyholdem.teacher.training --config configs/teacher_nfsp_250k.yaml --output runs/teacher-nfsp-250k-v1` (add --resume after interruption). This conventional worker can run alongside the one measured full neural worker.
Automatic approval review rejected publishing generated teacher result JSON files under the data-exclusion instruction. Keep those new files local and publish only source/configuration and human-written findings. Existing public history is preserved; no rewrite or deletion is planned. This does not block local experiments.

## Gate 2 confirmed; CI bootstrap fix verified
Full confirmation completed: five seeds, mean 0.8234375 versus frozen 0.471875 and shuffled 0.5; each paired one-sided sign-flip p=0.03125. Retention and exact erasure passed, as did an independent 9,865-operation journal/decoder/reward audit. Runtime 1,004.48 s / 717.6 MB RSS. Learned mechanism: competing-readout suppression; action-0 selected by the fixed legal tie rule, no strategic fallback. See updated docs/CONDITIONING.md. Generated evidence remains local in ignored runs/conditioning-full-confirm-v1; no new generated JSON is staged for publication.
CI on 3f3069a/43bf7cc failed because native integration tests ran before a module-specific test built the library. A session fixture now builds on a fresh checkout and otherwise verifies/reuses the existing binary; the ordering-dependent duplicate build was removed. Full local suite remains 66 passed / 2 skipped. An isolated checkout with no native binary passed both native integration tests after automatic initialization. No active experiment binary or src/ code changed. Commit/push this test fix and human-written conditioning evidence, verify CI, then tag conditioning-v0.
The independent 250,000-hand teacher run is active under runs/teacher-nfsp-250k-v1 (log sibling .log), code identity 3f3069a, execution commit 43bf7cc. Do not change src/, uv.lock or environment until it completes. Its 25,000-hand predecessor failed validation and remains barred from teaching. Next: validate the longer teacher, actual bounded-edge student transfer/Gate 2A, then gated poker curricula; original UI/native integration and human play remain required.

## Teacher v1 longer run failed; register v2 before execution
The 250,000-hand teacher completed in 1,253.76 s and failed the full development suite: +1.791 / +0.154 / −0.670 / −2.070 BB per hand against random/station/TAG/equity; only random passed its adjusted lower bound. Confirmation seeds remain unused and no corpus exists. See docs/TEACHER.md; generated results remain ignored/local.

V2 adds teacher-only visible-card features (354 inputs), retaining all other 250,000-hand settings and original seed. The first 237 inputs exactly match the native player's fixed encoder. New features contain no opponent cards, future sampling or teacher information. Complete suite 68 passed / 2 skipped, including exact v2 resume and private-information invariance. Commit/push these changes before execution. Snapshot this commit's src, uv.lock and configuration to ignored runs/teacher-runtime-v2, and run from that immutable snapshot. This permits independent implementation in the checkout without changing the active run's identity or dependencies.
Command: `PYTHONPATH="$PWD/runs/teacher-runtime-v2/src" .venv/bin/python -m flyholdem.teacher.training --config configs/teacher_nfsp_cards_v2.yaml --output runs/teacher-nfsp-cards-v2`. Resume adds --resume. Export and evaluation use the same PYTHONPATH. Do not change the installed app environment or snapshot. Next independent work: exact/tabular local transfer, native dashboard integration and required human-play engineering; dependent poker claims remain gated.

## Exact-cue circuit transfer implementation — register before execution
Added an exact two-token table teacher, separate teacher-free native cue driver, complete local transfer/control runner, actual-weight export and deletion audit. Full tests: 70 passed / 2 skipped. Observer-attached and observer-free native decisions are identical; teacher targets cannot enter the cue driver. Only existing KC-to-MBON edges may change. This is distilled-connectome teaching, not bio-plastic chip-reward training. Rate 0.3 comes from conditioning; the passing original circuit mapping is unchanged. Gate 2 confirmation is hash-checked as a prerequisite.
Commit this exact source/config before smoke and development. Commands and resume are in docs/TRANSFER.md. First output runs/exact-transfer-circuit-smoke-v1, then runs/exact-transfer-circuit-dev-v1 at profile development; both use --learning-rate 0.3 and --resume after interruption. No circuit result exists yet. The independent v2 poker teacher continues from immutable runs/teacher-runtime-v2; checkout edits do not change it. No environment changes or native rebuild are planned.

## Exact transfer development qualifies; actual teacher-removal check passes
Circuit development on 1a232a5: learned mean 82.3% versus 55.2% frozen/restored and 54.2% shuffled, 173.24 s / 134.1 MB. Retention passed. Smoke stop/resume journal byte-identical. Exported actual circuit model 1c4d40c3 reproduced all 64 held-out decisions after teacher/corpus deletion; changed cue source rejected and no teacher/learning/experiment modules imported. Details/hashes in docs/TRANSFER.md; generated evidence remains local. Complete Gate 2A is still pending.
Commit written evidence, then confirm on the reserved five seeds from immutable runs/exact-runtime-v1, profile confirmatory, rate 0.3, matching development reference and output runs/exact-transfer-circuit-confirm-v1. Exact command/resume in docs/TRANSFER.md. Preserve snapshot/environment/native binary; independent checkout work is allowed. V2 conventional poker teacher remains active in its own frozen runtime (last inspected 140,000 / 250,000 hands).

## Native dashboard verified; latest scientific failures preserved
Native frozen spectator evaluation is integrated with the existing table, clear fly/community insets, both chip piles and actual action gestures. Full cloud renders 166,700 retained neurons (139,662 annotated soma positions + 27,038 explicitly schematic missing-coordinate points); circuit supports 4,505. Counts, roles and annotation details are exact, five neural scores are the sole fly action source, weights stay frozen, and no reward is delivered. Replay checks graph/registration identity. Fixture replay switches back to the original schematic. Written evidence reports separate experiments with honest failed/pending labels.
Verification: 74 tests passed / 2 isolated-oracle skips; fixture desktop/mobile/live/replay and all avatar gestures passed again; full Chromium desktop/mobile, geometry hashes, native activity, filters, annotation, pause and fixture/native source switching passed with no console/page errors. New generated screenshots/reports remain local. Restore only the old tracked avatar review artifacts overwritten by this verification, after preserving new copies locally; publish source/config/docs/tests/scripts only.
Commands: `.venv/bin/flyholdem serve --mode full --model runs/conditioning-full-confirm-v1-model --port 8767`; default fixture `.venv/bin/flyholdem serve --port 8766`. Native verification `.venv/bin/python scripts/native_browser_check.py --url http://127.0.0.1:8767 --output runs/native-browser-check`. The actual full conditioning model has no validated poker strategy. Stop the full dashboard worker before starting another full-graph experiment.
Exact transfer confirmation: fail, mean 77.8% < registered 80%, frozen 54.2%, shuffled 55.5%, retention/erasure pass; no confirmatory export. V2 conventional teacher: fail, +0.977/+0.348/−0.523/−1.619 BB per hand across the suite, no positive adjusted lower bound; no corpus/teacher confirmation. See docs/TRANSFER.md and docs/TEACHER.md. All runs completed; frozen historical runtime bundles preserve exact resume/load identities. Next: nonbiological bounded-edge surrogate transfer plus a stronger conventional teacher candidate, followed by dependent curricula and required human play.

## Surrogate transfer registration — next execution
Native dashboard milestone 23bcd6f is published and CI passed (run 34686633854). A nonbiological direct-readout rate surrogate is implemented, using actual native forward scores and projected gradients only on the existing preregistered KC-to-MBON subset. No new edge/adapter/decoder or alternative action source exists. It delivers no dopamine. Finite-difference gradient checks, legal/direct-edge bounds and frozen-weight tests pass; full suite 76 passed / 2 skipped.
Commit exact source/config before execution. Ordered rates 0.1, 1.0, 10.0; unchanged circuit/cues from failed local transfer; new development and confirmation seeds; same 80% fidelity threshold. Smoke output runs/exact-surrogate-circuit-smoke-v1, then development rate 0.1 output runs/exact-surrogate-circuit-lr01-v1. Exact commands and resume are in docs/TRANSFER.md. Only continue to later registered rates after failure, and reserve confirmation for the first qualifying rate. Full/fixture spectator servers remain active; no second full-graph worker is planned. Conventional teacher improvement is still required; no validated teacher/corpus exists.

## Surrogate smoke recovery verified; development begins
The actual circuit smoke on a986b7f completed in 5.15 s. It registered 10,450 direct existing readout edges (subset hash d49261663694743fe90acf2a52fbae4aaadc00c99ad93c3d8474ab1253afad84). Its 40-trial accuracy was 50% across learned/frozen/shuffled, an engineering smoke only. Stop/resume at operation 30 reproduced the complete journal byte for byte: 9b78f00d7c5421ccd7ad6a6d0d3739c41c19ad5d2a9b236373a2856384401a3e.
Freeze source/config/uv.lock in ignored runs/surrogate-runtime-v1, with immutable references to the data, native binary and prerequisite artifacts. Start development rate 0.1 from this snapshot, output runs/exact-surrogate-circuit-lr01-v1. Command: `PYTHONPATH="$PWD/runs/surrogate-runtime-v1/src" .venv/bin/python -m flyholdem.experiments.exact_transfer --config runs/surrogate-runtime-v1/configs/exact_transfer_surrogate.yaml --profile development --learning-rate 0.1 --output runs/exact-surrogate-circuit-lr01-v1`. Add --resume after interruption. This permits independent teacher implementation without altering the active experiment's source identity. Do not change the snapshot, installed environment or native LIF binary.
A fast visible-card strength evaluator for the conventional teacher is being prototyped under the task workspace work/teacher-equity, not integrated or used for training yet. The fly's encoder is unchanged. Teacher candidates remain unvalidated.

## Surrogate qualifies; conventional teacher v3 registered
Surrogate development at the first registered rate 0.1 passed: 92.2% mean learned versus 53.1% frozen/restored and 54.7% shuffled-teacher, with retention/erasure; 148.20 s. Stop candidate search: rates 1 and 10 are not run. Exact command/resume for five-seed confirmation in docs/TRANSFER.md, output runs/exact-surrogate-circuit-confirm-v1, runtime snapshot runs/surrogate-runtime-v1. Preserve local-rule failure. No poker claim follows.
Teacher-only v3 adds fast deterministic visible-card Monte Carlo strength features, with no actual hidden cards or deck access. Its original C++ evaluator is separate from the native LIF binary. PokerKit remains canonical rules/settlement. Rank ordering/ties verified on 4,500 sampled hands plus special cases; full suite 83 passed / 2 skipped including 1,800 additional CI comparisons, v3 exact recovery, privacy and export identity. Config configs/teacher_nfsp_equity_v3.yaml keeps all other 250k v2 settings fixed.
Commit exact v3 source/config and written surrogate finding before executing. Snapshot v3 src/lock/config with a preserved equity binary in runs/teacher-runtime-v3; run command/resume in docs/TEACHER.md, output runs/teacher-nfsp-equity-v3. This teacher and the independent circuit confirmation may run together within measured resources. Do not change installed packages or the shared native/equity binaries. Independent source edits are safe because both runs use frozen snapshots.


## Private heads-up interface verified; surrogate component confirmed

Human play is implemented for fixture/circuit/full: independent frozen neural state, private random deal seeds, server-validated legal/revision-checked actions, human own cards visible, fly cards only if actually shown at showdown, and all neural/private diagnostics omitted from the public event stream. Both avatar rigs follow committed actions; reload reconstructs the current hand and last poses/captions; Live/Replay closes the match and resumes the spectator. Only one full-graph player advances at a time. This fulfills the interface engineering requirement; the available full model is still cue-conditioned and unvalidated for poker.

Verification: 88 tests passed / 2 isolated-oracle skips; native state isolation and no-fallback failure covered. Chromium fixture/full human play passed six hands each, private cards/diagnostics, legal buttons, reload, both players' motions, desktop/mobile and return to live. Full native rendering regression again passed all 166,700 points, checksums, exact activity and source switching. Avatar regression passed all six fly gestures, opponent check/call, clear community cards at 0/3/4/5, both balances/chip piles and full silhouettes. Generated reports/screenshots stay local; old tracked avatar review artifacts are restored after preserving updated local copies.

Small exact surrogate confirmation passed: five seeds, learned 94.53125% versus 55.15625% frozen and 52.1875% shuffled; both p=0.03125, retention and erasure exact. Independent 9,865-operation audit passed. The actual confirmed circuit model c3876173 reproduced 128 actual decisions after teacher/corpus deletion and rejected changed cue source. Scope is the small Gate 2A transfer/removal components only, not whole Gate 2A or poker learning. See docs/TRANSFER.md for complete written evidence and mechanism limits.

Conventional v3 completed 250k hands in 1,674.43 s and failed the same held-out development suite: +1.180/−0.348/−0.250/−1.588 BB/hand across random/station/TAG/equity; no adjusted lower bound positive. No validated teacher or corpus exists; confirmation seeds unused. Source/runtime snapshots remain immutable for all past results. Next diagnose conventional continuation values and policy averaging, register a stronger candidate, and implement independent gated curricula/CLI/reporting without claiming dependent gates passed.

Servers: fixture 8766 log runs/fixture-dashboard-human.log; full conditioned 8767 log runs/native-dashboard-human.log. Command `.venv/bin/flyholdem serve --mode full --model runs/conditioning-full-confirm-v1-model --port 8767`; click Play against fly. Human browser checks: `.venv/bin/python scripts/human_browser_check.py --url http://127.0.0.1:8767 --output runs/human-browser-full` (fixture uses 8766). Teacher v3 and surrogate runs are complete; no training worker remains. Do not rebuild shared native binaries while the full dashboard runs. Published v3 CI succeeded, run 34687486468. Commit/push this verified milestone; keep the broader goal active.


## Conventional v4 value-learning registration

Human-play milestone 91c1cc3 is published. V3 failed its fixed suite; training-deal-only inspection shows its average policy varies weakly with visible equity, but does not establish the cause. V4 is a controlled Double DQN change in the conventional teacher: online legal action selection, target-network value evaluation, zero terminal bootstrap. All other v3 250k settings and the entire original opponent suite are unchanged. No fly population/encoder/decoder/plasticity parameter changes.

Two added tests verify target action selection/legality/terminal behavior and complete Double DQN optimizer/replay recovery; full suite 90 passed / 2 isolated-oracle skips. Commit this exact source/config before execution. Then freeze src/lock/config/own equity binary in runs/teacher-runtime-v4 and run `PYTHONPATH="$PWD/runs/teacher-runtime-v4/src" .venv/bin/python -m flyholdem.teacher.training --config runs/teacher-runtime-v4/configs/teacher_nfsp_double_v4.yaml --output runs/teacher-nfsp-double-v4` (add --resume after interruption). Independent checkout work may continue after the snapshot is fixed; no environment/binary changes. Next independent work: unified CLI/reporting and gated poker curriculum engineering. Whole Gate 2A and all poker learning gates remain pending.


## Unified commands and verified recorded reports

Added actual CLI dispatch for preregister, conditioning train, exact-cue distill, conventional teacher train/export/evaluate/export-corpus/verify-corpus, and report. Existing module commands remain compatible. New CLI uses --resume RUN_DIRECTORY and creates timestamped runs when output is omitted; complete workflows emit reports. General poker train/evaluate remains subsequent implementation, not a completed gate.

The offline reporter generates Markdown/standalone HTML/JSON, verifies result/manifest/preregistration hashes and complete modern journal index/hash/head/count consistency, rejects truncated/nonfinite/mutated evidence, and labels old unchained controllability logs as checksum-only. It imports no teacher and loads no model. Actual conditioning confirmation, surrogate confirmation, failed v3 teacher evaluation and original circuit controllability reports generated successfully. Chromium desktop/mobile inspection passed with no overflow/errors; failed results stay failed. New generated reports stay local; source/config/docs/tests only are published.

Full suite: 99 passed / 2 isolated-oracle skips. New checks cover corrupted journal/head/manifest/nonfinite data, immutable source artifacts, escaped HTML, legacy limits, CLI resume contract and a real tiny NFSP CLI training run with report. Human-play CI 34689008868 passed on 91c1cc3. V4 Double DQN runs from immutable source 9588f0a, source hash abb1ac799f52e1abd2b1b8c3bbf094550df258394e13e22e4e3ead70f018b185, config hash 46b3fbb6eb41de7c357d5f1e93f28589d1bb31d93153729eff6fba728a11b5ed; last inspected 90,000 / 250,000. Keep snapshot/environment/equity binary unchanged. Resume command in previous section. Next: gated shove/fold, river and full-hand curriculum engineering while this teacher continues; whole Gate 2A remains pending teacher/corpus qualification.


## Four genuine poker environments and frozen paired evaluation — register before execution

Implemented 10 BB shove/fold, declared uniform river/check-call prefix, and 20/100 BB full-hand environments using PokerKit alone. Canonical masks, full-stack call representation, private checkpoint restore and byte-level hidden-hole/future-deck invariance pass. A teacher-free native evaluator measures the complete four-opponent suite on seat-swapped identical deals, logs/fsyncs every completed hand, verifies actual legal neural argmax and unchanged weights, and checkpoints all native state/RNG/progress with exact tail recovery. No frozen core inference file changed. The report/CLI support this baseline without labeling it as learning.

Full suite: 108 passed / 2 isolated-oracle skips. New tests cover all four curricula, actual native deterministic rollouts, sole-score action selection, exact stop/resume journals and correctly paired statistics. Config configs/frozen_poker_evaluation.yaml registers 32 pairs per opponent / 256 full-model hands, development deal seed 5100000, bootstrap 5190000, all four original opponents. Commit exact source/config before execution. Stop the full native spectator (8767) before running; preserve the fixture server. Snapshot src/lock/config and immutable data/native-binary/registration links in runs/frozen-evaluation-runtime-v1. Command: `PYTHONPATH="$PWD/runs/frozen-evaluation-runtime-v1/src" .venv/bin/python -m flyholdem.cli evaluate --config runs/frozen-evaluation-runtime-v1/configs/frozen_poker_evaluation.yaml --model runs/conditioning-full-confirm-v1-model --output runs/native-full-frozen-baseline-v1`; resume replaces --output with --resume naming the same run. Restore the full spectator after completion.

The independent Double DQN teacher v4 remains active from its own frozen runtime (last inspected 200,000 / 250,000). No installed environment or binary changes. Whole Gate 2A remains pending teacher/corpus; poker learning gates remain pending. This baseline measures the existing cue-conditioned model only and cannot select neurons, gains, decoder or biological plasticity parameters. Next: inspect the independent v4 teacher validation and implement gated learning/control runners after this engineering checkpoint.


## Full frozen baseline complete; v4 negative result and learning observer

Registered full baseline on 3c74016 completed 256 hands in 17.32 s, unchanged full model weights. Returns +0.773/+2.250/+0.367/+1.047 BB/hand against the original suite, but only fold (149) and all-in (71) among 220 neural decisions. This is a small development baseline, not poker learning or a gate pass; no biological parameter selection follows. Exact hashes/intervals in CURRICULA.md. Full dashboard restored on 8767, log runs/native-dashboard-after-baseline.log. Source snapshot runs/frozen-evaluation-runtime-v1 preserves reproducible replay.

Double DQN v4 completed 250k hands in 1,620.01 s and failed development: +1.064/−0.217/−0.264/−1.809 BB/hand; no positive adjusted lower bound. Private-information invariance passed. Frozen policy 372e5ee1, evaluation manifest bf99368c; no confirmation/corpus. All current experiments are complete, no training worker remains. Next conventional candidate requires a registered population-robustness change, not another untested claim or altered evaluation opponents.

Added a separate poker learning observer around the unchanged native inference core. Five new tests verify frozen decision/count bytes, actual coactive local/surrogate bounded updates, complete neural/eligibility/baseline/RNG recovery, and rejection of teacher inputs during frozen/bio-plastic operation. The deliberately coactive seven-neuron numerical fixture is not biological evidence. General gated poker training/control runner remains to implement. Full suite 113 passed / 2 isolated-oracle skips. Keep original frozen runtime files and core inference identities unchanged. Publish the checked observer and written findings, then register the next conventional training candidate.


## Conventional v5 population-prior registration

Teacher v1–v4 failures are preserved. V5 keeps v4 features/Double DQN/agent settings but trains 500,000 hands against a declared 50% self-play / 50% uniform original fixed-opponent prior. Both learned agents see both positions; fixed-opponent hands update only the active learned agent. No opponent identifier enters policy input, and the complete original validation suite/unused confirmation deals remain unchanged. This is explicitly NFSP with a fixed policy prior, not a PSRO/equilibrium claim. The target is robustness, with no biological parameter selection.

The teacher-only fast equity-opponent helper preserves v1 card order, visible hash, Python RNG samples, thresholds and action priority. Exact equity on 120 CI states and all four opponent trajectories match; 200 additional states matched in 0.03758 s versus 3.28761 s. Population checkpoint/journal recovery and actual frozen-policy export identity pass. Full suite: 116 passed / 2 isolated-oracle skips. Previous curriculum and observer CI passed (34690219599 and 34690728980).

Commit exact source/config, then snapshot src/lock/config and a separate copied teacher-equity binary in runs/teacher-runtime-v5. Run `PYTHONPATH="$PWD/runs/teacher-runtime-v5/src" .venv/bin/python -m flyholdem.teacher.training --config runs/teacher-runtime-v5/configs/teacher_nfsp_population_v5.yaml --output runs/teacher-nfsp-population-v5` (add --resume after interruption). The full and fixture dashboards are restored; this single conventional worker may run alongside the measured native viewer. No environment/shared binary changes. Next independent work: gate certification, actual poker learning/control runner and small tabular shove/fold teacher. Whole Gate 2A and poker Gates 3–6 remain pending.

V5 execution identity: source commit fcbf9670f6a1ae6783d751e36875cea034700c7d, frozen source SHA-256 8729d42267d58ee583286bd8ea6160220808dc1dc9229906a4dd33f7d4dc2628, configuration SHA-256 7edefa79117df280ef7d110b5fbc328200f9204d6b15470e5f45e240555e0d9c. Snapshot prepared and verified before execution. The preceding command is also the recovery command with --resume. Logs/checkpoints remain ignored.


## Teacher qualification recomputed from complete evidence

Published v5 source/config identity fcbf967 and hash checkpoint 7be7fd1 both passed CI (34691247924 / 34691292671). Population training runs from the unchanged frozen v5 snapshot; last inspected 60,000 / 500,000 hands. Resume uses the preceding v5 command with --resume; environment and binaries remain unchanged.

Corpus export no longer trusts result flags alone. It checks the exact registered suite/schedule, complete journal, numeric policy hashes and stack, recomputes bootstrap summaries and teacher qualification, and repeats the loaded teacher's actual information-boundary test. New CLI teacher verify-evaluation supports read-only negative/development audits without granting qualification. Real v4 development recomputed exactly across 512 paired deals and remains failed. Full suite 123 passed / 2 isolated-oracle skips; seven new numerical evidence checks reject changed flags/intervals, schedule, tensors, opponents and truncated logs. Generated artifacts remain ignored/local.

Next: complete-hand poker learning/control orchestration around the verified observer, with gate prerequisites enforced before biological execution. No teacher is qualified, no actual corpus exists, and whole Gate 2A/Gates 3–6 remain pending. The existing clear card strips, both chip piles, side-seated animated fly/opponent, and private heads-up interface remain verified/published.


## Complete poker-hand learning integration verified

Published qualification integrity source 921187d passed CI 34691566793. Added complete PokerKit hand orchestration around the unchanged native-score observer: selected action commits before teacher query, only one seat learns, actual terminal chip result and control overrides stay separate, and the reward baseline uses canonical position (button=1). Within-legal target permutations preserve target mass/entropy and are explicit in each training decision. Frozen operation rejects all teacher/shaping/exploration inputs and verifies unchanged weights.

Ten new native integration checks pass across all four curricula, exact settled-hand/count equivalence with the existing frozen evaluator, all three learning methods' state restoration, actual post-commit teaching order and control bookkeeping. Complete suite: 133 passed / 2 isolated-oracle skips. No biological poker training has run; this low-level mechanism must be used through the forthcoming gate-enforcing experiment orchestrator.

V5 remains active from frozen fcbf967 source, last inspected 120,000 / 500,000 hands; 63 GiB free storage. No environment/shared binary changes. Whole Gate 2A and Gates 3–6 remain pending. Next independent work: a separately labeled small tabular shove/fold teacher and full experiment prerequisite/control scheduling. The small teacher cannot substitute for the unchanged full 20 BB teacher validation suite.


## Small tabular shove/fold teacher registered and verified

Implemented separately labeled chance-sampled CFR over two public nodes and 169 own-card classes. Every sample traverses all three actual PokerKit terminal branches. Regret matching uses simultaneous pre-update strategies, opponent reach only in counterfactual regret, and own-reach strategy averaging. Eight tests cover all 1,326 classes, settlement, analytical toy games, hidden-information/domain boundaries, numeric source/runtime-pinned exports, exact replay from an older checkpoint, CLI and reports. Full suite 141 passed / 2 isolated-oracle skips. Details and primary algorithm references are in docs/SHOVE_FOLD_TEACHER.md.

Config configs/shove_fold_teacher.yaml registers 200,000 chance traversals, training deal seeds 6000000 onward, with no poker-based parameter or checkpoint selection. Export remains unvalidated and cannot qualify the separate full 20 BB teacher. Commit this source/config before a long run. Snapshot src/uv.lock/config in runs/shove-fold-runtime-v1. Command: `PYTHONPATH="$PWD/runs/shove-fold-runtime-v1/src" .venv/bin/python -m flyholdem.cli teacher train-shove-fold --config runs/shove-fold-runtime-v1/configs/shove_fold_teacher.yaml --output runs/shove-fold-teacher-v1`; resume replaces --output with --resume for that same run.

Measured 100 traversals at 0.20116 s / 53,641,216 bytes peak RSS. Current population teacher resident memory is 814,064 KiB at one CPU, full viewer 193,680 KiB resident, with 16 GiB host RAM and over 60 GiB free disk. This second small conventional CPU worker fits those measurements; it does not add a full neural worker or alter installed packages/binaries. V5 last inspected 240,000 / 500,000 hands. Whole Gate 2A and biological poker Gates 3–6 remain pending. Next: independently evaluate the small reference, finish gate-enforced biological orchestration, and inspect V5 only on its registered completed schedule.

Small tabular execution identity: source commit 3db455130623652394a00ec571377923ec23e53f, source SHA-256 c42bb29daea52e58747b2e41ded36703c7e9bcd6d0236d02ca49660283033c78, config SHA-256 5262c6e77be33a915631f34dcef37919372c4bf4a5064f3926690aa598871403. Snapshot fixed before execution; preceding command with --resume is its recovery command.


## Small reference completed; separate evaluation registered

Small CFR run completed 200,000 chance traversals in 427.01477 seconds, minimum 531 visits per class/node. Frozen policy 8a378ff881fc1bf11ae65f00687d271cafc05b847ef8dc7b117de8a91af5e1b1 was exported from the original immutable 3db4551 source. Training manifest 4f2d9b1e048c037230085e1722d8ea54afd65da75083d72b99338f406a630d2b. It remains unvalidated. Training and execution-identity CI passed (34692275951 / 34692298723).

Implemented the separate small-game evaluator: actual 32-state private-information mutation check, complete paired original-opponent suite, exact interrupted recovery, rederived bootstrap summaries, matching completed training provenance, and checked disjoint ranges. Config registers training 6000000..6199999, development 512 pairs/opponent from 6400000, confirmation 2048 from 6500000, boundary probes 6300000..6300015. Confirmation requires matching passing development evidence. Full teacher qualification remains false even if small-game confirmation later passes. Two new checks plus full suite: 143 passed / 2 isolated-oracle skips.

Commit exact evaluator/config before execution, then freeze src/lock/config in runs/shove-fold-evaluation-runtime-v1. Command: `PYTHONPATH="$PWD/runs/shove-fold-evaluation-runtime-v1/src" .venv/bin/python -m flyholdem.cli teacher evaluate-shove-fold --policy runs/shove-fold-teacher-v1-policy --training-run runs/shove-fold-teacher-v1 --config runs/shove-fold-evaluation-runtime-v1/configs/shove_fold_teacher_evaluation.yaml --output runs/shove-fold-teacher-v1-development`; resume replaces --output with --resume for that run. V5 population training remains active from its own frozen source, last inspected 350,000 / 500,000. Whole Gate 2A and biological Gates 3–6 remain pending; no environment or binary changes.

Small evaluation execution identity: source commit 5e46d4534fc1e1e4d9663cc7f0e2abc3f4cc5b24, source SHA-256 1c8e69ae6e1f294d489c32e64ba54765e2b028e997772e8324eed4144a43850e, config SHA-256 520875761a922e426b63336d84e1ccac4e6e6a5fcb346b7643bd3239ab056363. Snapshot fixed before evaluation; preceding command with --resume names exact recovery.


## Small-game development passed; same-policy confirmation next

The frozen small table 8a378ff8 passed development on all original opponents (512 paired deals each): +0.355469/+0.456055/+0.330566/+0.276855 BB per hand for random/station/TAG/equity, every Bonferroni-adjusted lower bound above zero. Actual 32-state information-boundary checks and independent schedule/statistic recomputation passed. Development result SHA-256 735383b3fb45e1f63de219a67907bf791e49698a68dd4b149dd648ebda2eeb0b, manifest 59cb41ad6bbf3bf041d7784c7c9585ad8d8a47f2b5624af1e306b2828f5ea457. No small-game teacher authorization until confirmation, and this result cannot qualify the full-hand teacher.

Continue unchanged source 5e46d45 (SHA 1c8e69ae6e1f294d489c32e64ba54765e2b028e997772e8324eed4144a43850e), config SHA 520875761a922e426b63336d84e1ccac4e6e6a5fcb346b7643bd3239ab056363, frozen table and predeclared confirmation deals: `PYTHONPATH="$PWD/runs/shove-fold-evaluation-runtime-v1/src" .venv/bin/python -m flyholdem.cli teacher evaluate-shove-fold --policy runs/shove-fold-teacher-v1-policy --training-run runs/shove-fold-teacher-v1 --config runs/shove-fold-evaluation-runtime-v1/configs/shove_fold_teacher_evaluation.yaml --profile confirmatory --development-reference runs/shove-fold-teacher-v1-development --output runs/shove-fold-teacher-v1-confirmatory`. Resume replaces --output with --resume naming that run. V5 remains active, last inspected 390,000 / 500,000; no environment or binary changes. Full Gate 2A/Gates 3–6 remain pending.


## Small-game teacher confirmed; full-hand teacher still pending

The unchanged tabular policy 8a378ff8 passed its registered 2,048 paired deals per opponent: random +0.349243, station +0.471558, TAG +0.349609, equity +0.254028 BB/hand, all suite-adjusted lower bounds positive. All 8,192 paired-deal rows/16,384 hands and bootstrap summaries verified; 32 private-information probes passed. Small-game-only qualification is true; full teacher qualification is explicitly false. Exact result SHA 5a224b6f3dc68b744a371a349b9cfbd2a9cd7f2b563632e874320922a780ef98, manifest 317c2eb4942aa5e897db0b26f524616da984b0ff3e10c9fc949d712e83642096. Full source/config/table identities remain unchanged.

Updated reports distinguish small-game qualification from full teacher approval; 18 relevant report/tabular checks pass after this display change, with the prior complete suite at 143 passed / 2 skips. Evaluator/identity/development CI passed (34692798998, 34692820593, 34692955404). README/model card now correctly list completed human play, confirmed surrogate transfer and the scoped small reference. A clear 169-class strategy PNG/SVG and full confirmation report were saved only to local task outputs; no generated files are staged for publication.

V5 is still active from frozen fcbf967, last inspected 460,000 / 500,000. Next: complete/export/validate V5 on the original full suite; whole Gate 2A and biological Gates 3–6 remain pending. General gate-enforced multi-seed poker/control orchestration is still required, beyond the verified complete-hand mechanisms. Do not select biological parameters or substitute the small teacher to bypass the full-hand qualification. No environment/shared native binary changes.


## Matched controls verified; completed V5 ready for original suite

Prepared explicit synthetic topology and shuffled-return controls. Endpoint permutation preserves source/destination degree, transmitter signs, initial edge weights and selected starting checkpoint multipliers, while a distinct graph hash rejects original-graph binding. Rewards shuffle within canonical position. Frozen training arms can share exploration temperature without learning; evaluation still rejects exploration, teachers and shaping. Two added integration checks pass; complete suite 145 passed / 2 isolated-oracle skips. General gate-enforced multi-seed orchestration is still pending, and no biological poker learning has run.

V5 finished 500,000 hands in 2,901.88463 seconds. Final checkpoint completed and exported from frozen fcbf967 source (SHA 8729d42267d58ee583286bd8ea6160220808dc1dc9229906a4dd33f7d4dc2628). Training manifest d3324b5fd51335d9f743d71d6698e2ed7084dc7296441c26379eabbdcf198697; journal head ab0c4b211e457210ff766a43b06039283384e87ae12124464dfba9bb1858cc0f; final policy c7f922e82890e7722ad43d9a73740f7e95ac62b775a4200aba54c52e478bca7b. Policy remains unvalidated; no full-hand corpus exists.

Original full-suite YAML SHA 5fb344fad4c27a5a5556764db0d0c43b1f64177ccadafc68b3d81453f52ca9c1 is unchanged. Execute `PYTHONPATH="$PWD/runs/teacher-runtime-v5/src" .venv/bin/python -m flyholdem.teacher.evaluation --policy runs/teacher-nfsp-population-v5-policy --config configs/teacher_evaluation.yaml --profile development --output runs/teacher-nfsp-population-v5-development` (add --resume after interruption). Do not run confirmation unless development qualifies. Small tabular confirmation remains a separate passed subgame reference; full Gate 2A/Gates 3–6 remain pending. No environment/shared binary changes.


## Full V5 negative result preserved; best-response component next

V5 evaluation completed from frozen fcbf967 and failed: random +0.568359, station +0.404297, TAG −0.283203, equity −1.480469 BB/hand; no adjusted lower bound positive. Private invariance passed 32 decisions. The stronger verifier independently recomputed all 512 pairs. Result SHA ddfe482d3163c0f53b562fea532c4375c33f3deea0dbb70c1810ad0b15f63743; manifest 22d60152a3b2303cf91089e4fa496648487c19794873cb6158ad415ffdd6aee4; journal d331baa428a975537f4eaefdf072fdeac28f8fcc6a492000c4578e7316cab4a5. No confirmation/corpus; no active training/evaluation worker remains. Fixture and full dashboards continue.

Next controlled candidate is a separately labeled equal mixture of the two final legal greedy Q best-response policies already learned in V5. This is not the historical NFSP average, nor an equilibrium claim. Implement a separate source/runtime-pinned numeric export and policy loader without modifying frozen prior policy modules/snapshots. Verify the completed training source, original compatible tensor codec/environment and final checkpoint before exporting; commit the extraction config and exact source before running the unchanged full development suite. Preserve all prior failures and do not choose any biological parameter from these results.

The small 10 BB CFR teacher remains confirmed only for its own subgame. Full Gate 2A, canonical full-hand corpus, biological poker training/control orchestration and Gates 3–6 remain pending. Matched control mechanisms were published at fb55dad; last full suite 145 passed / 2 isolated-oracle skips. Generated V5 negative reports were saved only in local task outputs.


## V6 Q-component extraction registered and tested

Added a separate conventional policy type: an equal mixture of each final V5 Double-DQN head's legal greedy action. It is explicitly a population-trained best-response mixture, not NFSP's historical average or an equilibrium claim. Extraction checks the fixed training manifest/source, original network/feature/codec files, environment and equity binary identity, final checkpoint and complete journal. It exports the exact saved Q tensor bytes, never retrains or changes biological parameters. Prior policy modules/snapshots and frozen fly inference remain unchanged; an explicit new loader routes full-hand evaluation/corpus to the appropriate conventional type. Statistical qualification and canonical information boundaries are unchanged.

Three new checks pass, including exact Q versus average-head identity, legal greedy probabilities, private invariance, corrupted tensor rejection and the original paired suite with correctly recomputed sampling labels. Full suite 148 passed / 2 isolated-oracle skips. Matched-controls and negative-result CI passed (34693506279 / 34693732839); no training/evaluation worker remains active.

Commit exact candidate source/config before extracting/evaluating. Freeze src/lock, configs/teacher_best_response_v6.yaml and configs/teacher_evaluation.yaml, plus a separate verified equity-binary copy, under runs/teacher-runtime-v6. Then execute `PYTHONPATH="$PWD/runs/teacher-runtime-v6/src" .venv/bin/python -m flyholdem.cli teacher export-best-response --config runs/teacher-runtime-v6/configs/teacher_best_response_v6.yaml --output runs/teacher-best-response-v6-policy`; followed by the same PYTHONPATH CLI teacher evaluate with --policy runs/teacher-best-response-v6-policy --config runs/teacher-runtime-v6/configs/teacher_evaluation.yaml --profile development --output runs/teacher-best-response-v6-development. Resume evaluation replaces --output with --resume naming that run. Extraction writes a new numeric export and is not a training run. Whole Gate 2A/Gates 3–6 remain pending; the confirmed small-game teacher cannot substitute for this full-hand qualification.

V6 execution identity: commit d9e1084904f5d2672992c2fc5b4cce57ea8388a1; source SHA 519f644b4c23abf3ce9649acaa6e26cbaea90eaa3461be2d41066985d4214216; extraction config SHA 57a5585de5629afde2df46aa444262b2f8bc8bac3ed80da3c63ce3983b7e792f; original evaluation YAML SHA 5fb344fad4c27a5a5556764db0d0c43b1f64177ccadafc68b3d81453f52ca9c1. Frozen runtime prepared before extraction/evaluation; preceding commands and evaluation --resume are exact recovery instructions.

V6 extraction completed with every source/checkpoint/journal check: frozen candidate policy SHA ec1c05e64a5c08fafab75bf98a8f13e551fd64d87173c40ef957797c235c1fc0. No retraining occurred. Proceed with the preceding registered development command from source 519f644b4c23abf3ce9649acaa6e26cbaea90eaa3461be2d41066985d4214216 and original evaluation YAML 5fb344fad4c27a5a5556764db0d0c43b1f64177ccadafc68b3d81453f52ca9c1; qualification is still pending.


## V6 failed; V7 public-stack potential registered

V6 final-Q mixture failed the original full development suite: random +1.531250, station +0.566406, TAG -0.738281, equity -1.376953 BB/hand. Only random had a positive adjusted lower bound; 32 information-boundary probes passed. Independent complete-journal/statistic verification passed. Result SHA 113493b21f23957cc91d3d657e9b70308274df1b3697c78238cf4f215e99e9ee; manifest 724b9ae88b917fac9cf1a01411092876bb2edc9ca75efb682288cf0d6164d7c2; journal ddd8d3444fa7855334b0758212d7557e136375b5ad784af5161df2353e746498. Generated negative reports remain local. Full confirmation seeds remain unused; no full teacher/corpus is qualified.

V7 keeps V5's complete 500,000-hand population/Double-DQN configuration and changes only the conventional replay reward to the public remaining-stack potential. Raw chip return plus Phi(next)-Phi(current), terminal Phi=0 and discount=1, telescopes to the original objective minus an initial-state constant. Every actual hand checks the identity; no fly reward or biological parameter changes. Three new checks pass, including actual PokerKit incremental chip flow, unchanged physical trajectories before optimization and exact interrupted training recovery. See docs/TEACHER.md for the proof, scope and primary source.

Fixed an intermittent WebSocket test race: a live subscriber may join immediately before reinforcement, so the assertion now waits for a decision followed by reinforcement. Twenty repeated live connections and the full events file passed. Initial V6 CI 34694077189 failed only this timing assumption; subsequent identical source runs 34694106765 and 34694222781 passed. No server or UI behavior was changed.

Commit source/config before freezing runs/teacher-runtime-v7 (src/lock/configs and copied equity binary). Then run `PYTHONPATH="$PWD/runs/teacher-runtime-v7/src" .venv/bin/python -m flyholdem.teacher.training --config runs/teacher-runtime-v7/configs/teacher_nfsp_potential_v7.yaml --output runs/teacher-nfsp-potential-v7`; add --resume after interruption. One conventional worker alongside the previously measured native viewer, no environment/shared binary changes. Complete suite: 151 passed / 2 isolated-oracle skips. Next independent work: actual gate-enforced multi-seed poker/control orchestration and Gate 2A certification. Full Gate 2A and Gates 3–6 remain pending.

V7 execution identity: commit 5a6ebe8781d33fbabe41f8b75d44cbd4cabe2f7f; frozen source SHA 6743ce3404940f7f63d2312afb9d4fd172ab3a6cb21421e3ef986220a4b8e44e; training YAML SHA f2a893a2184b9bfb79f7c9c0c969ab1706f324b70f9d158ba18f779839deecaa; original evaluation YAML SHA 5fb344fad4c27a5a5556764db0d0c43b1f64177ccadafc68b3d81453f52ca9c1. Snapshot and separate equity binary copy verified before execution. Preceding command with --resume is exact recovery.


## Qualified corpus must reproduce actual collection

Added teacher verify-qualified-corpus: rederive full confirmation qualification, repeat actual policy boundary probes, reproduce every complete PokerKit collection hand/target, registered sampling RNG, deduplication, stratum limits, first coverage boundary and sorted split bytes. Exact original corpus configuration and teacher/evaluation/run bindings are required. The cheaper structural verifier still cannot authorize teaching. Four real-collection engineering checks reject rehashed target, trajectory and coverage corruption; synthetic qualification exists only inside isolated fixture collection tests and is rejected by the public qualified boundary. Complete suite: 155 passed / 2 isolated-oracle skips.

V7 source and identity CI passed (34695582886 / 34695605244). Its immutable source 6743ce3404940f7f63d2312afb9d4fd172ab3a6cb21421e3ef986220a4b8e44e remains active, last inspected 80,000 / 500,000 hands. Recover with the preceding V7 command plus --resume; no environment/shared binary changes. No full teacher/corpus is qualified, no biological poker training has run. Next: gate certification joining actual evidence and teacher-removal verification, then the multi-seed experiment runner. Whole Gate 2A and Gates 3–6 remain pending.


## Gate 2A dependency certification and actual removal implemented

Added gate certify-transfer / verify-transfer with pinned configs/gate2a.yaml references, full teacher confirmation, reproduced corpus, complete cue schedule/statistic recomputation, failed-local-before-surrogate checks and actual isolated teacher/corpus removal. Existing certificates are reverified from all dependencies, not trusted as pass flags. The actual failed V6 teacher is refused before loading a biological model; no rejected certificate is written. No full Gate 2A certificate exists yet.

Every operation of full conditioning, successful surrogate transfer and failed local transfer recomputed exactly (9,865 each), preserving 82.34375%, 94.53125% and failed 77.8125%. Actual exported circuit model c3876173e83c739738706ee4bd2e0a28c7854b934e63fc67ffbaba90b9e6defb reproduced the selected cue checkpoint's 128 recorded decisions, then reproduced identical bytes after copied teacher/corpus deletion. SHA 6fb3340bc2982622e3d2d458530143f9c1b950a5d30e246e807e0ad88815d099; no teacher/learning/experiment imports. The complete audit is local; these checks do not rerun historical learning or upgrade any poker claim.

Five added CI checks use real tiny native decisions and reject rehashed schedule, native argmax, summary and evaluation-learning corruption. Full suite: 160 passed / 2 isolated-oracle skips. Source and inference-core identities remain separate; existing exported models remain compatible. Docs/GATES.md explains scope and exact commands.

V7 remains active from its unchanged frozen 5a6ebe8 source, last inspected 140,000 / 500,000 hands. At 10 minutes its resident memory was 1,182,368 KiB with 60 GiB disk free; existing full viewer was 102,768 KiB. No environment/shared binary changes. Resume with the previously recorded V7 command plus --resume. Next: general multi-seed poker/control orchestration using the now implemented certificate boundary; wait for the unchanged completed teacher schedule before evaluation. Full Gate 2A and poker Gates 3–6 remain pending.


## Terminal fine-tuning and actual frozen neural opponents verified

Enabled terminal-return fine-tuning while retaining the distilled-connectome origin label. That method accepts no teacher input and uses the same bounded local rule and past-only position baseline as strict terminal learning; actual fixture trajectories, weights and interrupted continuation match the strict terminal mechanism. Added hash-pinned frozen native snapshot opponents with own-information-only input, actual native legal argmax, complete opponent decision records and unchanged-weight checks. Shared mutable player/opponent weights are rejected, maintaining one learning seat.

Two added integration checks pass; full suite 162 passed / 2 isolated-oracle skips. Published corpus and Gate 2A source CI passed (34695976274 / 34696368167). No biological poker training or concurrent full-graph snapshot match has run. Existing model/inference-core files, environment and binaries remain unchanged.

V7 remains active on its immutable source, last inspected 220,000 / 500,000 hands; use its earlier recorded command plus --resume for recovery. Next: finish the actual multi-seed training/control orchestrator, including genuinely disconnected evaluation phases and registered curriculum endpoints. No full Gate 2A certificate, full teacher/corpus or poker-learning claim exists yet; Gates 3–6 remain pending.


## Frozen poker evaluation in an isolated runtime

Implemented evaluate --disconnected: minimal source-pinned native/PokerKit/evaluator package, separate verified binary and numeric model, no teacher source or bytecode caches, graph-only data link, active teacher-import and original project training-file denial. Both denials are checked before native inference. Resume verifies exact source/config/model/graph/binary inputs and retains complete-hand journals/checkpoints; parent interruption forwards to the worker.

Actual native fixture evaluation matched every ordinary journal byte and recovered exactly after seven of sixteen hands; changed isolated source is rejected. Full suite: 163 passed / 2 isolated-oracle skips. Also compared sixteen actual circuit-model hands (all four original opponents) through ordinary and isolated inference: every journal byte matched, weights unchanged, teacher denied. Isolation manifest SHA 7c68cf537166fcc306f0aba77f8537127815f2f1cc90b7d572931093092f7b5d; this short integration check is not a poker result or parameter-selection experiment. Local evidence: runs/disconnected-circuit-v1; output audit saved locally. No second full-graph worker or environment/shared binary change.

V7 remains active from its immutable 5a6ebe8 snapshot, last inspected 300,000 / 500,000 hands. Resume with the earlier recorded command plus --resume. General multi-seed orchestration still needs to combine the verified training observer, matched controls, frozen snapshot opponents and isolated evaluation phases into registered curricula/endpoints. Full Gate 2A and Gates 3–6 remain pending; no biological poker learning has run.


## Complete native training-arm execution and recovery verified

Added a private complete-hand arm executor with paired deal/seat schedules, registered exploration, complete player/plasticity/initial-state and teacher/control/opponent identities, fsynced hash-chain journals and atomic five-minute/shutdown checkpoints. Native dynamics, all eligibility traces, position baselines and controller RNG restore exactly; checkpoint-tail replay must reproduce prior bytes. The only selected checkpoint is the registered final boundary, never a poker-profit-selected intermediate state. Shared frozen neural opponents remain fixed and have no persistent cross-hand dynamics.

Seven integration checks cover six actual native variants (strict plastic/frozen/shuffled reward; local distilled; shuffled surrogate targets; terminal distilled), exact interruption after five of twelve hands, and invalid control/configuration rejection. Full suite: 170 passed / 2 isolated-oracle skips. No public ungated training command was introduced and no biological poker training was run. The top-level multi-seed gate/phase orchestrator and curriculum endpoint aggregation remain to implement.

V7 is active from the immutable 5a6ebe8 snapshot, last inspected 410,000 / 500,000 hands. No environment/shared binary changes. On completion export the final average policy from that same snapshot, then use its unchanged original development suite; no intermediate checkpoint or biological parameter selection. Recover training with the earlier V7 command plus --resume. Whole Gate 2A and Gates 3–6 remain pending.


## Synthetic control persistence verified; V7 completed

Added fixed channel-row shuffling and a separately labeled synthetic topology export. It preserves actual permuted endpoints/initial scaled strengths, writes its own prepared-graph identity, uses explicit shuffled-control mode and scale one on reload, and cannot load against the original MaleCNS graph. Exact reloaded native count/score/action bytes pass; original-graph substitution is rejected. Complete suite: 171 passed / 2 isolated-oracle skips. No biological poker training or shared binary/environment change.

V7 completed 500,000 hands in 2667.02444833 seconds from frozen source 6743ce3404940f7f63d2312afb9d4fd172ab3a6cb21421e3ef986220a4b8e44e (code commit 5a6ebe8781d33fbabe41f8b75d44cbd4cabe2f7f). Training manifest da1cce8ecd5aacc5d99fe93e6f59ee20128266e8983544913dc8cfffb5a6b1fe; final journal head 49dfb1cefd4a42c8314bb6c433e4802442ad77561e23c635ddd02415f1f1faa5; result SHA 61e1ae25b8c81ee5e176b60ed65f8998f1d6cc17de33facc69b1f1226b61a3ce. The final average policy was exported from the same original snapshot: 220447d6552d21addd1e6104a70135a40a7b2894365c24e192c95cb9464f44f4. It is still unvalidated. The teacher training worker has exited; dashboards remain available.

Run the original full development suite using `PYTHONPATH="$PWD/runs/teacher-runtime-v7/src" .venv/bin/python -m flyholdem.teacher.evaluation --policy runs/teacher-nfsp-potential-v7-policy --config runs/teacher-runtime-v7/configs/teacher_evaluation.yaml --profile development --output runs/teacher-nfsp-potential-v7-development`; add --resume after interruption. Original evaluation YAML SHA remains 5fb344fad4c27a5a5556764db0d0c43b1f64177ccadafc68b3d81453f52ca9c1. No confirmation until development passes. Full Gate 2A and poker Gates 3–6 remain pending; top-level multi-seed orchestration/endpoints remain to implement around the now verified execution components.


## V7 negative result preserved; held-out labels excluded; V8 registered

V7 failed the unchanged development suite: random +1.921875, station +0.656250, TAG -0.566406 and equity -1.759766 BB/hand. Only random had a positive adjusted lower bound. All 32 private-information probes passed, and all 512 paired-deal rows/statistics recomputed exactly. Result SHA aae02a66a80464ca9559a95a4507accefc5c57b03e5b284167ff848919f902cf; manifest 3d26726f4737d488166d6d91eda4be2180cfe2a1557fc0a863c37283f2dbdce8; journal 4211e6f493ceb9d6d665898d03ed890177121a3d6b30b038db5ed093d777baf7. Generated negative reports remain local. No confirmation/full corpus/biological poker training is permitted; no teacher worker is active.

Online distillation can now exclude every canonical validation/test ID before querying a teacher. Skipped targets are explicit in decision records, with unchanged native action selection and no supervision event. The training-arm executor carries the registered split restriction; misuse in evaluation or terminal-only learning is rejected. One actual native integration check observes both skipped and delivered targets; complete suite 172 passed / 2 isolated-oracle skips. The top-level gated curriculum orchestrator must select the train-only restriction; it remains to implement.

V8 is a separately registered use of the already tested Q-component extraction, bound to the completed V7 source/checkpoint via configs/teacher_best_response_v8.yaml. No retraining, analytical override or biological change. Commit before freezing runs/teacher-runtime-v8 (src/lock, extraction/evaluation configs, separate equity binary). Then run `PYTHONPATH="$PWD/runs/teacher-runtime-v8/src" .venv/bin/python -m flyholdem.cli teacher export-best-response --config runs/teacher-runtime-v8/configs/teacher_best_response_v8.yaml --output runs/teacher-best-response-v8-policy`, followed by the same runtime CLI teacher evaluate using --policy runs/teacher-best-response-v8-policy --config runs/teacher-runtime-v8/configs/teacher_evaluation.yaml --profile development --output runs/teacher-best-response-v8-development. Resume evaluation replaces --output with --resume naming that run. Original evaluation YAML and unused full confirmation deals remain unchanged. Full Gate 2A and Gates 3–6 remain pending; dashboards remain available.

V8 execution identity: commit 034df21213f496567565ede420a4a64ec2eb5004; frozen source SHA 38e3d99ec7e03eec17d4d871fad75d47037945f3d877c7b3f9c3d0eb27e59fee; extraction YAML SHA 57c43470425a38ee4f96b0c9afe69b4471a989701585a2c1a87624d64b28540d; original evaluation YAML SHA 5fb344fad4c27a5a5556764db0d0c43b1f64177ccadafc68b3d81453f52ca9c1. Snapshot and separate equity binary copy verified before extraction/evaluation; preceding CLI evaluation with --resume is exact recovery.

V8 extraction completed all source/checkpoint/journal checks. Frozen policy SHA 4188ecd4fd8c5a13b3b940c9045f435d5129980d9664e1dd4e29b6bacd09b0c0; no retraining. Proceed with the preceding fixed-source development command, then independently recompute its result. No candidate qualification or full confirmation is authorized yet.


## V8 failed; exact terminal fold-boundary candidate tested and registered

V8 failed the unchanged full suite: random -0.941406, station +2.101563, TAG +0.019531, equity -1.619141 BB/hand. Only station had a positive adjusted lower bound. All 32 private-information probes passed; independent complete-journal/bootstrap recomputation passed. Result SHA ec17ae060a7406424f18b7dba5df502a862e1a8368dc36d7a3970bfe4b79f438; manifest 1f063594818622efdbfa672dfd61747e991f6eb1d41ffed25b9f0378eae58be1; paired journal SHA ff099e4483eb34c0f421aa6ebca80d5e19b3892eb6abfb369c3440b0119f4921. Generated negative reports remain local. No full confirmation or corpus is authorized; no teacher worker remains active.

V9 uses the exact legal-fold value implied by V7's already registered public-stack potential: zero future shaped return. It copies every learned V8 Q tensor byte, replaces only the fold estimate by this known terminal boundary before legal greedy selection, and preserves the equal component mixture. It rejects nonfinite outputs before applying the boundary. The method is separately labeled conventional, not a fly readout, NFSP average or equilibrium claim. Export requires matching completed undiscounted own-stack-potential-v1 training, pinned V8 policy and V7 manifest. No retraining, opponent or biological parameter change.

Three new checks pass: exact/masked boundary selection, unchanged tensors and reward-definition bindings, private invariance, and the original full evaluator with correct recomputed sampling/qualification. Complete suite: 175 passed / 2 isolated-oracle skips. Freeze exact source/lock/configs and copied equity binary under runs/teacher-runtime-v9 after this commit. Then `PYTHONPATH="$PWD/runs/teacher-runtime-v9/src" .venv/bin/python -m flyholdem.cli teacher export-potential-boundary --config runs/teacher-runtime-v9/configs/teacher_potential_boundary_v9.yaml --output runs/teacher-potential-boundary-v9-policy`; evaluate through the same runtime with teacher evaluate --policy runs/teacher-potential-boundary-v9-policy --config runs/teacher-runtime-v9/configs/teacher_evaluation.yaml --profile development --output runs/teacher-potential-boundary-v9-development. Resume evaluation replaces --output with --resume for that run. Full Gate 2A and Gates 3–6 remain pending; general multi-seed curriculum orchestration/endpoints are still required. Dashboards and frozen fly inference remain unchanged.

V9 execution identity: commit 50f420598975fa27df58cb40e793e103d5a3e29f; frozen source SHA a7016c1a9d64af0da1c44c8184410e8191b0f5211b018fee340a37d8dd9e8af3; extraction YAML SHA ecdeb0bd96e2f89a395ffe8096d11698e9abb31c4ae8f64058a0680241127aa4; original evaluation YAML SHA 5fb344fad4c27a5a5556764db0d0c43b1f64177ccadafc68b3d81453f52ca9c1. Snapshot and separate equity binary copy verified before extraction/evaluation; preceding CLI evaluation with --resume is exact recovery.

V9 export completed with unchanged learned tensor bytes and all source/reward-definition bindings. Frozen policy SHA a15dacb25161be56227eb9ec8f581c932ccc1ba616ec8c492fd7b7ea0c6f65f0. Proceed with the preceding fixed-source development command; no qualification or confirmation is authorized yet.


## V9 failed with identical recorded paired outcomes

V9 policy a15dacb25161be56227eb9ec8f581c932ccc1ba616ec8c492fd7b7ea0c6f65f0 produced byte-identical 512 paired-deal records to V8: random -0.941406, station +2.101563, TAG +0.019531, equity -1.619141 BB/hand; only station had a positive adjusted lower bound. The exact terminal fold correction did not improve this evaluated candidate. Thirty-two information-boundary checks passed; independent summary recomputation passed. Result SHA 0fd06b781bfc89d5be2e26d310c0839934c95882d025df246a9d01d5e29c3931; manifest 0014d9020d29b762687570accd53681202e579732a57b468ca0234e5e3d2fbe9; paired journal SHA ff099e4483eb34c0f421aa6ebca80d5e19b3892eb6abfb369c3440b0119f4921. Negative reports remain local.

No conventional training/evaluation worker is active. Full teacher candidates v1–v9 remain failed; full confirmation deals are unused, no full corpus exists and no biological poker training has run. The small 10 BB CFR teacher remains confirmed only for its subgame. Full Gate 2A and Gates 3–6 remain pending. Latest complete suite: 175 passed / 2 isolated-oracle skips.

Next: complete the top-level multi-seed curriculum/control orchestrator and statistical endpoints around the verified training-arm executor, held-out target exclusion, frozen snapshot opponents, explicit null graphs, and isolated evaluation. The gate certificate must remain enforced before real graph poker training. For further teacher work, consider a justified counterfactual-regret method rather than assuming another post-hoc fold adjustment will help; no new algorithm/config is registered or running yet. Keep biological parameters and the full opponent suite unchanged. Dashboards remain available; no environment/shared binary changes.


## Gated multi-seed poker orchestration implemented and verified

Added `curriculum run` / `curriculum verify` for all four native PokerKit stages and both separately labeled learning modes. The exact protocol is configs/poker_curricula.yaml, SHA 3c4d239ab4e7384e288080366618a3f4ef39668ba5e53cbd07ed8f151b23142a. It fixes three development/five confirmation seeds, disjoint deal ranges, original held-out equity opponent, all matched controls, terminal-only stages 5/6, prior final-seed snapshots, and no profit-based seed/checkpoint selection. Biological/local parameters derive from confirmed conditioning; the surrogate derives from confirmed exact transfer. The public entry point reverifies Gate 2A and preceding-stage/development dependencies before native player construction or output creation.

The runner executes complete training arms, isolated before/after/control/retention/erasure evaluations, explicit null graphs, and an actual conventional reference on identical held-out deals. Numeric exports distinguish inherited changes from currently trainable edges. Evaluation verifies actual final weight bytes, visible observations, legal neural argmax, original opponent actions and full PokerKit settlement. Training audits verify matched shuffled returns and past-only baselines. Independent-seed BB/100 bootstrap/sign-flip endpoints require improvement, all controls, held-out generalization, exact retention/erasure and registered noncollapse/shove-strength behavior. Fixture evidence cannot qualify a public curriculum. These checks do not rerun historical native learning.

Source-snapshot evaluation now explicitly denies teacher/corpus/parent-run paths outside the source checkout as well as original project training files. Actual denial probes execute before inference. Frozen inference core identity remains 1b2c99883afc6e6e30b65e8cf9aafef456953177c10caa443a8d62d531da09ca; no encoder/decoder/native solver/model/UI/environment/binary change. Aggregation retains compact endpoint fields plus full-record fingerprints, while private complete trajectories remain on disk. Completed training resumes without rewriting results; partial child phases retain their complete-hand recovery. Parent and child process RSS peaks are recorded separately.

Eight added integration/statistical checks cover two actual native fixture seeds and all controls, exact interruption after three phases, the full distilled train-only-target path, complete conventional reference replay, inherited-edge serialization, rehashed-score corruption, seed-level comparisons and prerequisite ordering. Additional isolated-evaluation coverage proves an external teacher directory is denied. CLI forms and generated fixture reports were checked. Complete suite: 183 passed / 2 isolated-oracle skips, 65.45 seconds. Earlier V9 source/identity/negative-result CI all passed; new milestone CI follows publication. No actual full-graph poker training or extra full native worker was run.

Working command after a real Gate 2A certificate exists: `.venv/bin/flyholdem curriculum run --stage 3 --profile development --learning-mode bio-plastic --gate2a runs/gate2a.json --output runs/poker-bio-stage3-development`; repeat separately with distilled-connectome. Resume uses the same arguments and `--resume runs/poker-bio-stage3-development` instead of --output. Confirmation adds --profile confirmatory and --development pointing to a matching passed development run; stages 4–6 also require --previous with the preceding confirmed gate. Full details and resource scheduling: docs/POKER_TRAINING.md. Stop/measure the advancing full viewer before actual full training or snapshot matches; preserve immutable source/config/lock/binary and prerequisite references for long-run recovery.

The remaining blocker is scientific: full teacher v1–v9 failed, full confirmation remains unused, no full corpus or Gate 2A certificate exists, and no biological poker learning claim is permitted. The small CFR teacher is still subgame-only. Next: a separately justified counterfactual-regret conventional teacher candidate, with algorithm/information-boundary/recovery checks and source/config registration before any long training. Primary MCCFR literature and OpenSpiel's external/outcome-sampling implementations were inspected; no new teacher algorithm/config/run has been implemented or started yet. Existing dashboards and human play remain available.


## V10 conventional external-sampling regret teacher implemented

Implemented a separately labeled tabular response to a stationary uniform population of the original four opponents, with actual PokerKit counterfactual branches, full own-action enumeration, regret matching and own-reach-weighted strategy averaging. The visible-equity abstraction retains earlier street buckets and every public action/paid chip; no hidden cards, opponent identity or hand seed enters its key. This fixed-opponent averaging construction is scoped explicitly and is not a self-play equilibrium claim. Source/config: configs/teacher_external_regret_v10.yaml and docs/EXTERNAL_REGRET_TEACHER.md. Thirty thousand traversals, alternating seats, seed 91001, deals 1,000,000–1,029,999, eight buckets and 64 hypothetical equity samples are fixed before execution.

The runtime binds the original evaluation YAML and rejects overlap with its development, confirmation and boundary deals before output creation. Numeric regret/average/key/RNG checkpoints retain complete-traversal boundaries; recovery reexecutes journal tails exactly. Complete recovery preserves the existing result. Only the fixed final iteration exports; the atomic frozen policy uses numeric arrays and an explicit legal-uniform prior for unseen information sets, including untrained stack domains. Capacity/node ceilings fail rather than prune. Original teacher qualification, historical NFSP/Q/potential modules and frozen native inference remain unchanged.

Bounded engineering probes: eight untrained trees / 1,307 nodes in 0.871370 seconds, 54,870,016-byte peak RSS; a separate sixteen-update arithmetic/recovery check covered 2,608 nodes / 996 terminal branches and 32 information-boundary decisions. They provide no strategic result or long-run forecast. Five integration tests cover actual small-game counterfactual arithmetic, full-game numeric/RNG/hash recovery, atomic export, original qualification recomputation, legal/private-history integrity and reserved-deal refusal. Complete regression suite: 188 passed / 2 isolated-oracle skips in 69.90 seconds. The final seed-isolation guard and all five new integration checks passed. No registered V10 training or evaluation has started.

After publishing this source/config, freeze src/uv.lock/configs and a separately verified existing equity binary under runs/teacher-runtime-v10. Begin the same fixed run with `PYTHONPATH="$PWD/runs/teacher-runtime-v10/src" .venv/bin/python -m flyholdem.cli teacher train-regret --config runs/teacher-runtime-v10/configs/teacher_external_regret_v10.yaml --output runs/teacher-external-regret-v10 --stop-after 128`; measure resources, then replace --output/--stop-after with --resume runs/teacher-external-regret-v10. Export/evaluate from that snapshot only. Full confirmation remains unused until original development passes. Full Gate 2A and Gates 3–6 remain pending; gated curriculum orchestration is implemented. Dashboards/human play remain available; no environment or shared binary changes.

V10 execution identity before training: code commit c5696202e887a57dd75ebe77f25c13466b8fdf3f; immutable source SHA f1c78c77620a834cd968672f224447e0ac910b0c95d3f36f645193cd4f490dff; training YAML SHA 6a2fd35755db99c5ab124d82ae0d105660cd78e295fa83e5ab619411cd5bcc9b; original evaluation YAML SHA 5fb344fad4c27a5a5556764db0d0c43b1f64177ccadafc68b3d81453f52ca9c1. Verified separate equity binary SHA 80846bbc3357982b6310ef05c3c8f9921621c0caa9db1d0c17a1c54cc43207ea. Snapshot runs/teacher-runtime-v10 contains exact src/lock/config bytes and a separate binary copy; environment/shared binaries are unchanged. The preceding frozen-runtime command starts at the registered 128-traversal resource boundary, then resumes the same fixed run.


## V10 resource boundary measured; optional evaluation dependency corrected

The first 128 registered traversals completed in 11.780402 seconds: 16,842 counterfactual nodes, 6,446 terminal branches, 2,383 information sets, 63,258,624-byte peak RSS and 856 KiB total run storage. Training manifest 7b41e4ba99eea406c9ada054c1f5881c8a793a61cbd7cab39b42f36b50245d38; complete-traversal journal head 20cb181d8bf6e1130bbcf6082ae40a5e6908f46716aab17cef5f9b8f266dd9a6. The run is deliberately interrupted at that registered boundary; no held-out result or qualification exists. Resources permit continuation of the same fixed 30,000-traversal schedule. The full dashboard health check passed and no second full native worker was started.

Source CI found that the shared full evaluator still imported optional PyTorch for tabular policies. The current checkout now enables PyTorch determinism only for neural teacher kinds. An actual tabular train/export/original-evaluation/qualification test explicitly denies every torch import, even on a host where torch is installed. Historical neural policy implementations and their deterministic evaluation settings remain intact. The frozen c569620 training snapshot is unchanged; this host already has its locked PyTorch dependency, so its original evaluator remains executable with identical strategy/game semantics. Thirty focused teacher checks passed in 11.26 seconds, including all existing full policy types and explicit no-PyTorch tabular evaluation. Corrected CI follows publication.

Resume the fixed original source/config with `PYTHONPATH="$PWD/runs/teacher-runtime-v10/src" .venv/bin/python -m flyholdem.cli teacher train-regret --config runs/teacher-runtime-v10/configs/teacher_external_regret_v10.yaml --resume runs/teacher-external-regret-v10`. Keep generated state local and export only the completed final iteration. Full confirmation, corpus and biological poker training remain gated.


## Published control evidence now visible in the dashboard

Added separate responsive comparisons for full local conditioning, failed local exact-cue transfer and circuit surrogate transfer. They display the already published five-seed means on a common scale, paired-difference confidence intervals, explicit graph/method/confirmation labels, retention/erasure details and experiment links. The 77.8% local-transfer failure remains failed, and the small-cue scope / pending full teacher gate remain explicit. Written summary values were checked against all three original result files; no raw experiment data, models or new gate claims are published. Entry assets are versioned so existing browser tabs load the updated CSS/JS.

Actual native dashboard inspection at 1440×1080 and 390×844 verified all nine values, passed/failed/passed labels, readable layouts, no horizontal overflow and the 128-decision teacher-removal disclosure. Existing automated browser coverage now checks those same result labels/values. JavaScript syntax passed; all twelve focused event/native-dashboard/private-play service checks passed in 2.32 seconds. Corrected teacher CI 34703637303 passed the complete fixture/data/oracle/live/replay/avatar/human-browser workflow. Fixed stale README/GATES wording that still described the already completed curriculum runner as pending.

V10 training continues from the unchanged registered c569620 snapshot and same saved run. Last inspected 5,700 / 30,000 traversals; 53,119 information sets, no held-out evaluation yet. The original frozen-runtime --resume command above remains exact recovery. Environment, shared binaries, biological parameters and full qualification opponents remain unchanged. Full Gate 2A and Gates 3–6 remain pending.


## Full teacher confirmation prerequisite enforced in code

Closed a public workflow gap: full teacher confirmation previously relied on the written instruction to pass development first. CLI and module evaluation now require --development-reference, rederive the complete matching-policy development suite before policy loading/output/deals, reject failed/nondevelopment/overlapping prerequisites, and bind exact dependency hashes in the confirmation manifest. Resume and independent confirmation/corpus/Gate 2A verification repeat that dependency. Historical development artifacts remain unchanged and independently readable; no unbound full confirmation can authorize teaching. Original opponents, sampling, PokerKit rules, bootstrap endpoints and thresholds are unchanged.

Three added tests cover missing/failed prerequisites before model/deal execution, CLI rejection, cycle/hash-change rejection, and actual tabular numeric export/PokerKit confirmation/recovery using an explicitly synthetic tiny development prerequisite. The original project registration rejects that numerical fixture. An actual V9 dispatcher probe reverified the failed result 0fd06b781bfc89d5be2e26d310c0839934c95882d025df246a9d01d5e29c3931 and refused confirmation before creating any output; all reserved full confirmation deals remain unused. Complete regression suite: 191 passed / 2 isolated-oracle skips in 80.57 seconds.

V10 continues its fixed 30,000-traversal run from immutable c569620 source/config; the checkpoint/RNG/journal recovery command above is unchanged. After training, export only its fixed final model from that original snapshot. Before any development/confirmation, freeze the current checked evaluator source plus original evaluation YAML in a separate V10 evaluation snapshot and record their identities. Its loader will verify the unchanged regret-policy implementation; this avoids changing an active training runtime while ensuring confirmation uses the new enforced dependency. No biological parameters, environment or shared binaries changed. Full Gate 2A and Gates 3–6 remain pending.


## Consolidated current-results index

Added docs/RESULTS.md and a local linked report bundle summarizing the working visualization/private-play interface, measured gates, all three cue/control comparisons, full-teacher failures and active V10 scope, test/resource evidence, working commands and primary result locations. Every listed result SHA and linked report file was checked. This is an explicitly interim evidence index; it does not mark the incomplete scientific release or poker gates complete. README links to it. No generated per-trial data or models are included.

V10 last inspected 12,700 / 30,000 traversals, 88,632 information sets, approximately 192 MB resident process memory. It continues from the unchanged registered snapshot and same checkpoint/journal. The evidence-panel milestone CI 34704215961 passed; confirmation-guard CI 34704599911 was still running at this documentation checkpoint. Full Gate 2A and Gates 3–6 remain pending; retain the earlier exact training resume command.

V10 evaluation runtime is now frozen before any held-out evaluation: execution code commit b06d7601d067bf0c0d9411e3a4fd2e2e424f0552; source SHA 1ab477e9fbb22b1f11f975e176f51dd9a5d4e04d23ac0bbf84a1b83b8d577c61; original evaluation YAML SHA 5fb344fad4c27a5a5556764db0d0c43b1f64177ccadafc68b3d81453f52ca9c1; regret-policy implementation SHA 857a8bedcea999e2ca6be6b41d2183d2030c44fdec838f71846735d8acb237ff. The implementation hash matched independently in both the active training snapshot and new evaluation snapshot. Existing binary bytes were copied and verified; neither environment nor shared binary changed. After final training export, use `PYTHONPATH="$PWD/runs/teacher-evaluation-runtime-v10/src" .venv/bin/python -m flyholdem.cli teacher evaluate --policy runs/teacher-external-regret-v10-policy --config runs/teacher-evaluation-runtime-v10/configs/teacher_evaluation.yaml --profile development --output runs/teacher-external-regret-v10-development`; resume replaces --output with --resume for the same run. Only a passing independently reverified development result allows the same snapshot's --profile confirmatory with --development-reference.


## Lossless native training activity retained for replay

The training hand recorder previously retained only a full spike-vector checksum. Future records now capture the actual vector before action/teaching as bounded compressed sparse int32 index/count pairs, preserving native dtype, neuron count, total spikes and the original full-vector SHA. Codec runtime and actual graph size are in the training manifest. The decoder rejects malformed, duplicate/out-of-range/nonpositive counts, excess decompression output, trailing bytes and checksum mismatches; silence round-trips exactly. Curriculum audits reconstruct the vector and reproduce its registered rates/scores. Only compact reward/seat rows remain in memory between matched-control arms after full audit.

Thirty-two focused native learning/recovery/curriculum checks passed in 49.05 seconds. Existing frozen outcomes/actions remain equal, both learning modes recover the same state/journals, and a synthetic full-length sparse vector verifies storage under 20 KB without a full native worker. The first uncompressed version passed the full 192-test suite. The final compressed format passed the complete suite: 193 passed / 2 isolated-oracle skips in 82.04 seconds. No teacher or neural policy logic, environment, shared binary or current viewer is changed. A training-run-to-viewer adapter remains a separate next step; captured activity is now available for it without guessing or neural reruns.

V10 last inspected 23,000 / 30,000 traversals with 129,842 information sets. It continues from immutable c569620 training source; guarded evaluation is separately pinned at b06d760 source 1ab477e9fbb22b1f11f975e176f51dd9a5d4e04d23ac0bbf84a1b83b8d577c61. Confirmation-guard CI 34704599911 passed. Export only the fixed final training model, evaluate through the already frozen guarded snapshot, and keep full confirmation unused unless development passes. Full Gate 2A and Gates 3–6 remain pending.


## V10 fixed final policy exported before development

The registered run completed all 30,000 traversals, 4,151,160 counterfactual nodes and 1,580,726 terminal branches, producing 152,391 information sets. The resumed invocation took 3,262.040563 seconds, in addition to the initial 11.780402-second resource boundary; peak RSS was 284,524,544 bytes. Run storage is 149 MiB and the numeric policy is 35 MiB. These training quantities are not held-out poker results. Complete-journal, fixed population/deal sampling, numeric checkpoint, RNG, source and runtime export checks passed. Only the registered final iteration was exported.

Frozen policy SHA 762b180986a3983e8a43f4744c6d89cf11299e16b611df6aba2e8b62a4206955; training result ba31db6f19ecda2e54ed15875cf025394181c35e3d2df0c39ae0c03a69ad49a2; manifest 7b41e4ba99eea406c9ada054c1f5881c8a793a61cbd7cab39b42f36b50245d38; complete journal file b1cb28ae6c03a190ebfbfce7b4161451943f0f5604ea22399692105158107e0b; journal head 9b7e1bb3df640c37c1801014f12fb9584a16c8071809349c726fa0a399b47659. Training source remains f1c78c77620a834cd968672f224447e0ac910b0c95d3f36f645193cd4f490dff from code snapshot c569620; its recorded launch commit c745be2 contains the subsequent execution-identity documentation.

Proceed with the already frozen guarded evaluator: `PYTHONPATH="$PWD/runs/teacher-evaluation-runtime-v10/src" .venv/bin/python -m flyholdem.cli teacher evaluate --policy runs/teacher-external-regret-v10-policy --config runs/teacher-evaluation-runtime-v10/configs/teacher_evaluation.yaml --profile development --output runs/teacher-external-regret-v10-development`. Resume replaces --output with --resume naming that same run. Independently verify through the same snapshot with teacher verify-evaluation --run runs/teacher-external-regret-v10-development --policy runs/teacher-external-regret-v10-policy --allow-development. No held-out result exists at this checkpoint. Full confirmation remains unused until development passes and its artifacts reverify. Full Gate 2A and Gates 3–6 remain pending.

Latest activity-recording milestone e3a7866 passed CI 34705921821, including fixture/data/oracle/live/replay/avatar/human-browser checks. The frozen native viewer and environment remain unchanged. Generated models, logs and reports stay local.


## V10 failed development; longer unchanged-method V11 registered

V10's fixed final policy 762b180986a3983e8a43f4744c6d89cf11299e16b611df6aba2e8b62a4206955 completed the original 128 paired development deals per opponent. Results in BB/hand, with suite-adjusted bootstrap intervals: random +1.843750 [0.535156, 3.167493]; calling station +2.480469 [1.506812, 3.407727]; tight-aggressive +0.328125 [-0.478040, 1.100134]; equity-bucket -0.013672 [-1.121094, 1.142627]. Only random and station have positive lower bounds. The full suite failed; the policy is barred from teaching. All 32 hidden-hole/future-deck/teacher-label checks passed. Independent complete-journal/statistical recomputation passed. Result SHA 9aa3b092046284baf4f3801f62af956d57e7f394816855675584b73d4e3ca37f; manifest e9df1243b6beac3137c977e262f91f9b65197bdbb880e7129878e39e5f235f39; paired journal 7c99f473bc77a7bafe287aac3ef83b86d965f3a581331f4c9d483a16d2f8bbf7. Reports and generated records remain local.

A read-only diagnostic replay reproduced all 512 paired records exactly. Of 1,948 actual teacher decisions, 23 states were unseen and 11 seen states had zero own-reach average mass. River coverage was thinner: 13/179 unseen, and 164/166 seen river decisions used states visited at most 100 times. Across the entire training table, visit quartiles were 1/1/3 and 123,129/152,391 states had zero own-reach average mass because full counterfactual branching also visits zero-reach continuations. These counts motivate a duration experiment; they do not establish a causal explanation of the failed strategic result.

V11 changes only the fixed horizon from 30,000 to 100,000 traversals in configs/teacher_external_regret_v11.yaml (SHA bcab450348d8aeb9abba80ddd6e35f0a9b47bd876a331a394ffbb7b23aa764c2). It restarts from the same initialization and sampling/deal sequence, so the first 30,000 operations should reproduce V10; it does not modify or relabel V10's completed run. Same algorithm, full public/private-bucket recall, eight equity buckets, 64 samples, original equal opponent mixture, averaging, stack depth and final-only export. Training deals 1,000,000–1,099,999 remain disjoint from all reserved development/confirmation probes. There is no biological or opponent selection from poker profit. This is a separate development candidate, without a strength or equilibrium claim.

After checked publication, freeze exact src/lock/configs and a separate existing equity binary in runs/teacher-runtime-v11. Execute `PYTHONPATH="$PWD/runs/teacher-runtime-v11/src" .venv/bin/python -m flyholdem.cli teacher train-regret --config runs/teacher-runtime-v11/configs/teacher_external_regret_v11.yaml --output runs/teacher-external-regret-v11 --stop-after 128`; verify this prefix against V10, then resume the same command with --resume runs/teacher-external-regret-v11 instead of --output/--stop-after. The same frozen source includes the enforced development prerequisite for confirmation. Export only the fixed final model, publish its identity before the original development suite, and independently verify the result. Full confirmation remains unused; no full corpus or Gate 2A certificate exists. Gates 3–6 remain pending. Current dashboards and human play remain available. No environment/shared binary changes.

V11 registration checks: all five existing external-regret integration tests passed in 5.84 seconds. Parsed configuration comparison confirmed that only iterations changes; the original evaluation YAML remains unchanged. V10 actual qualification recomputation and all 512 paired replay comparisons passed; its scientific outcome remains failed.

V11 execution identity before training: code commit 62bf21f297d14842181e56441210b62b79613d49; frozen source SHA d16984f8442b6fb8fec62f962ff4992d74dc3bedbca69ee7da017249a60ed48a; training YAML SHA bcab450348d8aeb9abba80ddd6e35f0a9b47bd876a331a394ffbb7b23aa764c2; original evaluation YAML SHA 5fb344fad4c27a5a5556764db0d0c43b1f64177ccadafc68b3d81453f52ca9c1; separate existing equity binary SHA 80846bbc3357982b6310ef05c3c8f9921621c0caa9db1d0c17a1c54cc43207ea. Snapshot runs/teacher-runtime-v11 contains exact committed src/lock/configs and the verified binary copy, including the enforced full confirmation dependency. Use the preceding 128-traversal start and same-runtime --resume commands. No V11 training or held-out play has occurred yet.

V11 first 128 traversals completed in 13.601229 seconds at 62,652,416-byte peak RSS. All 128 complete journal records are byte-identical to V10, including 16,842 nodes, 6,446 terminal branches and journal head 20cb181d8bf6e1130bbcf6082ae40a5e6908f46716aab17cef5f9b8f266dd9a6. New manifest SHA 1276bb4b27357c9b33a9ed08ae3e048e6ac0ec896d3a82f6c110b783a99b88a3. Continue the same fixed 100,000-traversal run using the recorded immutable-runtime --resume command. No V11 evaluation or full native poker training is active.


## Recorded training decisions bound to actual poker and action RNG

Added a solver-free recorded-hand audit used by the curriculum verifier. It reconstructs the actual seeded PokerKit hand and river setup, verifies the correct pre-action visible observation and exact encoded hash, restores each lossless native spike vector, reproduces rates/scores, checks the legal mask/silence/source labels, and replays the continuous registered action RNG at the scheduled temperature. It checks every committed action, action count, private replay checkpoint and final payoff. Original scripted opponents are replayed exactly; frozen snapshot opponents bind their own visible-observation hashes, reported legal argmax and snapshot identity. The latter checks reported scores, not unavailable historical opponent spike vectors. Existing source, matched-control, teacher-target and weight-continuity checks remain in the enclosing verifier.

Fifteen added integration cases cover both seats, all four curricula and three teaching/learning methods; continuous RNG state matches the original player after both hands. Native solver and teacher calls are explicitly forbidden during audit. Changed observations are rejected even with recomputed input hashes; changed sampled actions, commitment amounts, temperatures, private decks, missing decisions and rehashed frozen-opponent input records are rejected. All 23 focused replay/curriculum checks passed in 43.96 seconds. Complete regression suite: 208 passed / 2 isolated-oracle skips in 80.85 seconds. Frozen inference identity remains 1b2c99883afc6e6e30b65e8cf9aafef456953177c10caa443a8d62d531da09ca. This is record-consistency verification, not independent historical neural execution or a scientific learning claim.

V11 continues the fixed 100,000-traversal schedule from immutable code 62bf21f / source d16984f8442b6fb8fec62f962ff4992d74dc3bedbca69ee7da017249a60ed48a. Last inspected 3,600 traversals. The exact --resume command above remains valid. V10 failed the original full suite; full confirmation deals remain unused, Gate 2A and Gates 3–6 remain pending. The remaining training visualization task is a viewer adapter that consumes these audited records, preserves both player seats/private-card boundaries and labels historical teaching methods honestly; no training-run viewer is claimed yet.


## Audited historical native training viewer implemented

Added `serve --training-run <experiment>/<seed>/<arm>/training --graph <exact-prepared-graph> --start-hand 0 --hands 16`. It checks the enclosing curriculum/config, complete child evidence and actual prepared/scaled graph, then builds anatomical or explicitly synthetic graph buffers without constructing a neural solver. It consumes verified saved spike vectors and exact encoded input, original sampled actions and PokerKit settlement. Both original seats map to the visible fly; unrevealed/folded opponent cards, private checkpoints, deck order and premature outcomes remain outside viewer events. The fixed river setup is separately labeled and omitted from the neural-action trail, while exact observation history is retained. Display range is bounded to 128 hands; the complete initial audit still reads the full training arm.

Historical profile, matched control arm, actual opponent, stack depth, teacher connectivity and learning mode are explicit. Surrogate updates are labeled nonbiological; unavailable eligibility/dopamine and cell-level pulse records are not fabricated. Actual pulse population/total summaries are shown when recorded. The historical service rejects human-play creation; existing live/private-play services remain intact. Corrected the obsolete Three shadow constant to the same current fallback and versioned the changed avatar asset for existing tabs.

Twelve new integration tests cover all four curricula/both seats, all matched control arms and their exact graphs, local/surrogate methods, exact saved counts, private/folded cards, range/graph refusal, WebSocket/graph routes and forbidden native/teacher/live-player construction. Twenty-two focused viewer/native/private-play/event checks passed in 5.64 seconds; final twelve-viewer checks passed in 26.29 seconds. Complete regression suite: 220 passed / 2 isolated-oracle skips in 138.67 seconds; final JavaScript syntax checks passed. Actual CUA inspection at 1440×1080 and 390×844 verified clear card strips, chip balances, method/teacher labels, no horizontal overflow and disabled play. Two tiny real native fixture recordings (eight river hands each) remain local under runs/training-viewer-fixture-local-v1 and runs/training-viewer-fixture-surrogate-v1; these are numerical engineering fixtures, not poker-learning evidence.

Recorded preview commands are the new serve command with those fixtures' experiment/51/plastic/training and graph directories, on ports 8768 and 8769. Original full dashboard and private play remain on 8767; fixture dashboard remains on 8766. Latest published audit CI 34707187916 passed. V11 continues its unchanged 100,000-traversal run from source d16984f8442b6fb8fec62f962ff4992d74dc3bedbca69ee7da017249a60ed48a; last inspected 28,800 traversals. Resume remains `PYTHONPATH="$PWD/runs/teacher-runtime-v11/src" .venv/bin/python -m flyholdem.cli teacher train-regret --config runs/teacher-runtime-v11/configs/teacher_external_regret_v11.yaml --resume runs/teacher-external-regret-v11`. V10 failed, full confirmation is unused, and Gate 2A / Gates 3–6 remain pending. No environment or shared binary changed.


## Bounded native training journals and streamed replay audits

Native training opts into an indexed journal: only 64-bit file offsets and 32-byte hashes remain resident, while detailed hand/activity records are decoded on demand. Append bytes and per-hand flush/fsync remain identical. Resume streams the full file, detects an incomplete final line and verifies every chain entry; indexed access rechecks its bytes, and checkpoint-tail re-execution still requires exact equality. Other journal callers retain their existing row interface. No active frozen runtime was edited.

The native training verifier now streams every complete hand, retaining compact reward/seat schedules or only the requested replay window. It still reproduces all actual PokerKit observations, native spike-derived scores, continuous action RNG, control rewards, teacher splits and weight continuity. It verifies the final count/head/weights before returning any records. A full file digest binds the consumed bytes and current file to the preceding chain audit. Missing or corrupted late hands are rejected even when they are outside the display window. This bounds retained detailed-log memory; full audit time still grows with hand count.

Seven indexed-journal unit cases passed in 2.33 seconds, including byte equality, exact tail recovery, content/order/truncation failures, post-index changes, replacement during streaming and an eight-MiB bounded-memory check. Thirty combined journal/viewer/native-training cases passed in 40.70 seconds. Complete suite: 231 passed / 2 isolated-oracle skips in 166.84 seconds. Frozen inference identity remains 1b2c99883afc6e6e30b65e8cf9aafef456953177c10caa443a8d62d531da09ca.

Synthetic storage measurements: 128 records / 8,418,230 disk bytes used 286,128 peak traced Python bytes. At 1,024 records / 67,348,222 disk bytes, indexed write/resume used 322,314 Python bytes and 29,229,056 process peak RSS bytes; resident recording/resume used 68,535,459 Python bytes and 128,811,008 peak RSS bytes. The large files are byte-identical, SHA e4c4e300323ea8a86f63d0ab75d3fc89a914e0e3c534ccc3ef1328031c65336c. Generated benchmark files stay in ignored runs/journal-memory-*-v1 directories. These are storage measurements, not full neural training resources or poker results.

The preceding viewer milestone af6ed4928c192bf4928bd44c2319c06f654aedd6 passed CI 34709757391, including oracle and browser checks. Model card, training guide and dashboard evidence text now include the preserved V10 failure and active V11 scope. The first 30,000 complete V11 journal records exactly match V10, with prefix SHA b1cb28ae6c03a190ebfbfce7b4161451943f0f5604ea22399692105158107e0b and head 9b7e1bb3df640c37c1801014f12fb9584a16c8071809349c726fa0a399b47659. V11 last inspected 33,200/100,000 traversals. Continue the same source d16984f8442b6fb8fec62f962ff4992d74dc3bedbca69ee7da017249a60ed48a using the immutable-runtime resume command above. No intermediate policy was exported or evaluated. Full confirmation remains unused; full Gate 2A and Gates 3–6 remain pending. No environment or shared binary changed.


## Exact PokerKit evaluation reuse for future conventional traversals

Added a conventional-training-only helper that shares immutable standard Card values during deep copy and caches PokerKit's own StandardHighHand.from_game results within one counterfactual tree. The 256-entry ordered-card cache retains original PokerKit result types and combinations, is cleared on traversal exit/failure and is installed only on copied tree state. Mutable game state and history remain independent. PokerKit still performs every actual rule transition and payout; no poker scores or decisions are substituted. Future conventional policy manifests include the helper in their source identity.

A warm-cache prototype comparison on the first 64 training traversals used ordinary/candidate/candidate/ordinary order. Times were 8.755648 / 4.268350 / 4.283391 / 8.661164 seconds, with 7,989 nodes and identical complete-row fingerprint 63412300bbc84d67fa9f02e97bd3a00eae52f5c85133326f14dad1588205bfde and numeric table fingerprint 143c7eeccaa9a57b9c2661c038c2d1288f88139be74bfa99e2b3411a8781b5c8. This small workload approximately halved runtime; no long-run duration or policy-strength claim follows. The final helper reproduced every complete value in the first 128 original V10/V11 records (2,383 information sets), head 20cb181d8bf6e1130bbcf6082ae40a5e6908f46716aab17cef5f9b8f266dd9a6 and original prefix file SHA 6fe1de304408cfa25103e6390697eac4b053e92af78455f59d5ec36e5bc54395. Only training deals were inspected.

Six new differential cases cover all original opponents and both seats, fresh/restored decks, complete regret traces and numeric arrays, all legal branches across streets, parent/sibling isolation, original hand types/cache clearing, royal-board splits and all-in aces versus kings. All 22 focused rule/regret/source/export/recovery checks passed in 11.99 seconds. Complete suite: 237 passed / 2 isolated-oracle skips in 140.47 seconds. The storage milestone bc7324d869e0fa7bb94b15ea7faeb3126960da2e passed CI 34710332283.

V11 continues its original source d16984f8442b6fb8fec62f962ff4992d74dc3bedbca69ee7da017249a60ed48a; last inspected 40,700/100,000 traversals. Do not resume that run through the new helper. Its exact original export/evaluate/resume commands remain above. Any later corpus or native orchestration using a V11 policy must retain its exact frozen implementation modules and record the composed runtime source; loader source checks must remain strict. There is no V11 held-out result, full confirmation remains unused and Gate 2A / Gates 3–6 remain pending. The next independent integration check is tabular-teacher corpus export/replay with optional Torch imports disabled; its currently hardcoded pytorch-cpu provenance label needs the actual policy backend. No environment, shared binary or active model changed.


## Tabular corpus backend and dependency check

Corpus provenance now records the loaded policy's declared inference backend, matching evaluation, instead of always labeling it pytorch-cpu. Actual numeric regret-policy collection, canonical split/target replay, completed resume and rehashed-target rejection pass with Torch imports blocked. A separate fresh-process execution confirmed that no Torch module was loaded before or after these operations. The test alone substitutes a qualification guard to reach collection; restoring the real verifier rejects the artificial flags before a new corpus directory is created. This does not qualify any full teacher or create a scientific teaching corpus.

Twenty relevant tabular/neural corpus, regret-policy and qualification checks passed in 13.48 seconds. The previous complete suite passed 237 tests with two isolated-oracle skips in 140.47 seconds, and traversal milestone a93c8b1b1ff7e5b21629659df2b95081af8e00a6 passed CI 34711208643. Existing frozen source snapshots retain their own metadata and implementation; this correction applies to future corpus execution snapshots.

V11 last inspected 44,200/100,000 traversals and continues source d16984f8442b6fb8fec62f962ff4992d74dc3bedbca69ee7da017249a60ed48a. Preserve its original fixed-final export and original development/confirmation schedule. No held-out V11 result or full corpus exists; full confirmation remains unused, Gate 2A and Gates 3–6 remain pending. Next, prepare a downstream execution snapshot that combines checked current orchestration with the exact original V11 policy implementation modules, preserving source checks, and preflight it without using held-out deals or running native poker learning. The active training runtime, environment, binary and main viewer remain unchanged.


## V11 downstream source composed and preflighted without native execution

Prepared ignored runs/poker-runtime-v11 from checked orchestration commit 52016bfde9efae1611a6969a961f02d3d98f5da5, overlaying the seven exact original V11 policy implementation files. Composed source SHA 217828684efa5b9bf54dd4bf6bfd58d193f6e1813722156c8915a580b886e0ab; copied file ledger ca10a3e67d931b4ff05a6078464de0968d869a7fd07b2812a44b327387c661fa; unchanged original policy implementation 857a8bedcea999e2ca6be6b41d2183d2030c44fdec838f71846735d8acb237ff. SOURCE.json records all copied files/configs/binaries and ten named hash-checked prerequisite links. Native binary copy 44f5ce277154c953b213f41105b0d7f61e40af0cdd8d685289609c10b06be897 and equity binary copy 80846bbc3357982b6310ef05c3c8f9921621c0caa9db1d0c17a1c54cc43207ea verified without rebuilding. Original evaluation YAML remains 5fb344fad4c27a5a5556764db0d0c43b1f64177ccadafc68b3d81453f52ca9c1; corpus YAML 5e2c85c9b588bae55fa2f7bd50651fa7000d041288df1a18a84e86464e265241; native curriculum YAML 3c4d239ab4e7384e288080366618a3f4ef39668ba5e53cbd07ed8f151b23142a.

Actual frozen V10 loaded in original and downstream runtimes; all 32 training-deal probability records matched (fingerprint 8e580b8d10f4173d6b1d75e863056776832e8d6e7fd7d7ee80e70160c257cda2), with no Torch module loaded. Both complete prepared graph file sets and graph hashes verified, retaining circuit 4,505/1,018,725 and full 166,700/25,582,938 neurons/edges. Existing full conditioning, successful surrogate transfer and failed local transfer all reverified. The small 10 BB teacher's actual original confirmation and all 32 information-boundary checks passed again within its restricted scope. Actual fixture training replay produced nine events with SparseBrain construction forbidden. Gate 2A rejected V10 development before certificate creation. These are runtime/evidence compatibility checks, not new scientific qualifications; no native solver or full poker training was executed.

The corpus correction 52016bf passed CI 34711608110. The original V11 training source d16984f8442b6fb8fec62f962ff4992d74dc3bedbca69ee7da017249a60ed48a is unchanged and last inspected at 47,900/100,000 traversals. Continue only its original resume/fixed-final export/evaluate commands. If and only if the original development and reserved confirmation pass independently, use the new downstream runtime for the exact corpus/gate commands in docs/DOWNSTREAM_RUNTIME.md, then the same source for native curricula. Full confirmation remains unused; no V11 policy export, full corpus or Gate 2A certificate exists. Gates 3–6 remain pending. Current viewers and environment are unchanged.


## Poker strength prioritized; V12 fixed-final extraction registered

The user's observed push/fold limitation is confirmed from the current full spectator log: 30,472 complete events, 7,606 settled hands and 7,630 fly decisions; fold 3,804, check/call 0, half-pot 23, pot 0, all-in 3,803. Folds/all-ins account for 99.7%; no decisions were silent. All 23 flop actions were all-ins. Prefix journal head 8f2fe43afa14014c381179378c7e3ae04eb1e2bae273eedf231db52f99358455; frozen model 9e0f94abdd1cce92fc93c16dbbe9597d99456b01c3d09076784d2e51f8cf0259. These are descriptive calling-station spectator counts, not a paired or human-play qualification. The visible policy remains the two-cue conditioning model. Further weight-panel work was deferred before any repository edits in order to prioritize real poker strength.

Added a separate conventional final-positive-regret policy kind and export-final-regret CLI. This normalizes the learned final positive regrets over all legal actions, with the existing uniform prior for nonpositive/unseen states. It does not claim last-iterate convergence, equilibrium or improved strength. V12 extraction YAML a3bd05a204bb8603ffd3e0e5946e3ff27bc2214619c97c2e4832e593c68d356a pins V11's exact manifest 1276bb4b27357c9b33a9ed08ae3e048e6ac0ec896d3a82f6c110b783a99b88a3, source d16984f8442b6fb8fec62f962ff4992d74dc3bedbca69ee7da017249a60ed48a, configuration identity 20ccf17530b068046051e8b2ccc85115642475a8a6fe7bcb17675687d9eadde9 and fixed final 100,000 iterations. Original V11 average export/training and all earlier policy kinds remain unchanged.

Extraction rechecks every complete journal/RNG operation, final counts, array checksums and checkpoint boundary. The exported exact regret tensor retains its checkpoint checksum; loading recomputes every probability and rejects a rehashed strategy substitution. Runtime/source identities remain strict. Actual numeric candidate collection/replay works without Torch, and the restored real qualifier rejects test-only artificial flags. Existing confirmation/development gates apply unchanged. An actual attempted extraction from active V11 refused before creating output. This is a recovery/extraction check, not a policy trial.

Twenty-six focused checks passed in 8.66 seconds. Complete suite: 248 passed / 2 isolated-oracle skips / 2 existing dependency deprecations in 94.52 seconds. Tests include exact final-table normalization, all five legal actions, historical-artifact independence, hidden-card/deck/label invariance, source/config pins, interrupted-run refusal, changed sampling/checkpoint RNG, rehashed probabilities, real PokerKit evaluation, confirmation refusal and both tabular corpus kinds. Frozen inference matches the exported model runtime exactly: whole identity 1b2c99883afc6e6e30b65e8cf9aafef456953177c10caa443a8d62d531da09ca, source component 0ff3dd35407182b978284b95c4bd4a2256cf092a8686979b79c8fd2e6d00e12f. No environment, biological parameter or shared binary changed.

Previous stable c5810cb3132759ea74a05492cd833c0953749486 passed CI 34712134403. V11 last inspected at 69,100/100,000 and continues its original immutable source. Its 53,317-step checkpoint also passed a read-only complete-prefix/array/population/deal-RNG audit, head b566b2977ecd978015331a4978be4506c3f90c718edc9df183f14c9aa7c0b498. Resume only with `PYTHONPATH="$PWD/runs/teacher-runtime-v11/src" .venv/bin/python -m flyholdem.cli teacher train-regret --config runs/teacher-runtime-v11/configs/teacher_external_regret_v11.yaml --resume runs/teacher-external-regret-v11`, and only after the current job is authoritatively terminal.

Next: freeze the checked V12 code/config/binary in its own conventional runtime. Complete V11's original fixed-final average export, publish its identity before original development, and verify that result. If V11 development fails, use the exact V12 export/evaluate/verify commands in docs/POKER_STRENGTH.md. If V11 passes, proceed with its existing confirmation and native-training path. Do not export intermediate policies or spend full confirmation without independently passing development. No V11/V12 held-out result, full corpus or Gate 2A certificate exists. Gates 3–6 remain pending. Keep the main goal active; a candidate implementation is not a stronger released fly.


V12 conventional runtime is now frozen and preflighted at runs/teacher-runtime-v12. Code commit 0a1a6c7cc9d7a602796db4c789875c8356853687; source SHA 38b9e758a439054a701f22917a2bc5516b0d84f885d517eab8a5a813d7dfea0f; file ledger 13e329b06e516bd64ae63126cafc0484c198e09cda997c868e440d91c7c9b17c. Its complete file ledger and original evaluation YAML 5fb344fad4c27a5a5556764db0d0c43b1f64177ccadafc68b3d81453f52ca9c1 verified. The separate existing equity binary is 80846bbc3357982b6310ef05c3c8f9921621c0caa9db1d0c17a1c54cc43207ea; it was copied, not rebuilt. A fresh process from this exact runtime passed real tiny numeric extraction/evaluation/qualification tests in 2.06 seconds, with Torch absent before and after. It also rejected active V11 before policy output creation. No full model or held-out trial was produced by these preflight checks.

V11 was authoritatively live at PID 56954, 99% CPU, after 72,900/100,000 traversals and 257,072 information sets. Treat this as a verified wait on its fixed completion condition; do not restart from a stale log or observation timeout. Preserve the preceding original average-first and conditional V12 commands. The broader goal remains active and the visible fly is not yet improved.


## Reviewed V12 checkpoint binding and actual teacher-play audit

An independent bounded review demonstrated a valid-key reassignment bypass in V12: the original regret tensor was checkpoint-bound, but rehashing two swapped canonical keys with matching legal masks reassigned their strategies. Export/load now bind key_offsets.npy, key_bytes.npy, legal.npy and regrets.npy to their original final-checkpoint checksums. An actual tiny trained-table regression rejects that key swap and incomplete provenance. Eleven focused final-regret tests passed in 4.88 seconds. No actual V12 model exists; the original V11 training and earlier V12 snapshot were not edited. The old V12 snapshot must be superseded by a newly frozen runs/teacher-runtime-v12-reviewed before real export; extraction config and method remain unchanged.

Added standalone scripts/audit_teacher_play.py, run through each policy's original frozen PYTHONPATH. It first verifies the registered complete evaluation, independently executes every pair with the exact PokerKit opponents and RNGs, requires exact saved payoffs/action counts, and reports separate check/call/raise frequencies by street/opponent. It rechecks all evidence after consumption and grants no qualification. Seven independently written tests passed in 6.46 seconds: both numeric policy kinds, real original-evaluator action/payment observations, all four streets, both seats, exact repeatability, immutable artifacts/source, no Torch, coherent forged-record refusal and no CLI replacement registration.

Actual read-only V10 replay through the compatible original V11 runtime reproduced all 512 paired records and 1,948 decisions: fold 89, check 423, call 109, half-pot 244, pot 820, all-in 263. Decision trace b442acaa3248115e6aec5db3efcbfee9c53a807c565d734d8716472b4fc9bac7; audit script e272b0e67668f7fa1387fdbca4ba2296f45560d50fbe0d4e7850e90514bbcef3. The V10 development failure and native fly's push/fold limitation are unchanged. This audit uses no new held-out deals and does not substitute a conventional policy for neural scores.

Prior documentation milestone 9881af06af3a38c834c4b0f9b471d08cb07252ca passed CI 34714103928; V12 registration 0a1a6c7cc9d7a602796db4c789875c8356853687 passed CI 34713858690. Original V11 was last observed at 81,500/100,000 traversals and continues PID 56954 from immutable d16984f8442b6fb8fec62f962ff4992d74dc3bedbca69ee7da017249a60ed48a. Its original average-first export/evaluate/audit order remains mandatory. Conditional V12 commands now name the reviewed runtime, to be frozen after checked publication. No full confirmation, new qualified teacher/corpus or native poker model exists; full Gate 2A and Gates 3–6 remain pending. Keep the full poker-strength goal active.

Complete integration verification for the checkpoint-binding fix and actual-play audit: 257 tests passed, with two isolated-oracle skips and two existing dependency deprecations, in 99.59 seconds. No browser-facing code, neural policy parameters, active runtime or environment changed.


## Reviewed V12 execution runtime ready

The reviewed V12 runtime is frozen at `runs/teacher-runtime-v12-reviewed` from checked commit `a97506f6dbad5bd3ea33c5439a3b59dcbc41c199`. Source SHA `f70a256a49f9e80274290f1835cb1f9f20d84abb9e573dceff95f4fdb3dd373a`; file ledger `8392816361bf644c0a3469349283ca7e906cf8972aa7ccd9c7bdeeb561c542b2`; policy implementation `f6aa18842893bfceb4783ebbac827697cf957c18caf6a230e231491c0d9f1d98`. Extraction configuration remains `a3bd05a204bb8603ffd3e0e5946e3ff27bc2214619c97c2e4832e593c68d356a` and the original evaluation configuration remains `5fb344fad4c27a5a5556764db0d0c43b1f64177ccadafc68b3d81453f52ca9c1`. The copied existing equity binary remains `80846bbc3357982b6310ef05c3c8f9921621c0caa9db1d0c17a1c54cc43207ea`. All frozen files verified. Four actual tiny extraction/evaluation/replay checks passed in 7.11 seconds through this exact runtime, including key-rebinding refusal; Torch was absent before and after, and original V11 source was unchanged. No full model or new held-out trial was produced by preflight. Use this reviewed runtime for conditional V12 execution; retain the older snapshot only as history.

The checkpoint-binding/play-audit milestone passed CI [34714885981](https://github.com/kosh2shmood/flyholdem/actions/runs/34714885981), including the fixture, oracle and browser jobs. The complete local suite remains 257 passed / 2 isolated-oracle skips in 99.59 seconds. These checks establish engineering integrity, not poker strength.

Original V11 was authoritatively live at PID 56954, 99.1% CPU, with 91,600/100,000 traversals and 291,814 information sets. Continue the fixed run; export only after terminal final checkpoint completion through original runs/teacher-runtime-v11. Original average-policy identity publication, development, independent verification and actual-play reproduction come first. Conditional V12 follows only if that development fails. No full confirmation, qualified corpus or native poker model exists; full Gate 2A and Gates 3–6 remain pending. The full goal stays active and the visible fly remains the cue-conditioned baseline. No environment, shared binary or active model changed.


## Reviewed V12 native-orchestration path prepared

The immutable local downstream snapshot runs/poker-runtime-v12 uses checked a97506f source f70a256a49f9e80274290f1835cb1f9f20d84abb9e573dceff95f4fdb3dd373a and final-regret policy implementation f6aa18842893bfceb4783ebbac827697cf957c18caf6a230e231491c0d9f1d98, with no source overrides. Copied file ledger b1350b32a5b5ccf42f6f5141306e99b309a695223e9ea3f08a7ba606f9d75708; existing native binary 44f5ce277154c953b213f41105b0d7f61e40af0cdd8d685289609c10b06be897. Corpus and curriculum registrations remain unchanged. Actual preflight and conditional commands are recorded in docs/DOWNSTREAM_RUNTIME.md. No full teacher qualification, corpus, Gate 2A certificate or native poker model was created. Original V11 average-first evaluation and conditional reviewed V12 order remain in force; full Gate 2A and Gates 3–6 remain pending. The goal remains active.

The exact downstream preflight passed in 3.25 seconds: 83 tiny fixture corpus rows from 17 hands, 64/12/7 canonical splits, all four streets, complete target/trajectory reproduction and unchanged completed resume. Original cue outcomes pass/pass/fail and the limited small teacher reverified, with no Torch or native worker. Existing failed V10 development was refused before policy load or certificate. Full frozen inference remains 1b2c99883afc6e6e30b65e8cf9aafef456953177c10caa443a8d62d531da09ca. Prior documentation commit 520eea3f0e08403cba9f001ea216585eb8d32999 passed CI 34715486678.

Original V11 was authoritatively live at PID 56954, 98.1% CPU, at 97,700/100,000 traversals. An immediate local final-export waiter (exec session 58209; projectless work/mccfr/wait-v11-final-export.py, log work/mccfr/v11-final-export.log) now observes that exact PID, refuses observation errors or identity changes, and exports only the original average policy after the process is absent and final result is complete. It does not evaluate. Do not launch another export while that waiter is active. Once export completes, verify and publish the final policy identity before the original development suite, then independently verify and reproduce actual play. The original immutable PYTHONPATH and conditional candidate order remain unchanged.


## V11 fixed final average exported before development

Original V11 completed all 100,000 traversals, 13,858,172 counterfactual nodes and 5,275,792 terminal branches, producing 306,943 information sets. Its resumed invocation took 11270.364090 seconds; peak RSS was 545,734,656 bytes. The policy contains 74,592,670 disk bytes. These are training/resource measurements, not held-out poker results. The original immutable runtime's complete final-table export and an independent repeated provenance/checkpoint/array/RNG audit passed; recorded node and terminal totals also matched the complete journal. No Torch loaded.

The frozen original average policy SHA is `52bd9fbf298031415bf03f99072c4a144539d0f4f620452ee03f2aaf65f22df7`. Training result `4852e3905060ea000ec6663b5486c6b6236680ae2008454bc781cec82c2dde6f`; manifest `1276bb4b27357c9b33a9ed08ae3e048e6ac0ec896d3a82f6c110b783a99b88a3`; complete journal `8969685cc3337ed22a62b128ed2975d40f3eae5f4d9959c1267614742a6c47e9`; journal head `fb7911e24214ad47b4eb911d9c4f89526a03d07a6e59c6d26716b36b1f5ecb6e`. Original training source remains `d16984f8442b6fb8fec62f962ff4992d74dc3bedbca69ee7da017249a60ed48a`. Only the registered final iteration was exported, and its identity is recorded before development. No V12 policy has been exported.

After publishing this identity, run the original development suite:

```sh
PYTHONPATH="$PWD/runs/teacher-runtime-v11/src" .venv/bin/python -m flyholdem.cli teacher evaluate --policy runs/teacher-external-regret-v11-policy --config runs/teacher-runtime-v11/configs/teacher_evaluation.yaml --profile development --output runs/teacher-external-regret-v11-development
PYTHONPATH="$PWD/runs/teacher-runtime-v11/src" .venv/bin/python -m flyholdem.cli teacher verify-evaluation --run runs/teacher-external-regret-v11-development --policy runs/teacher-external-regret-v11-policy --allow-development
PYTHONPATH="$PWD/runs/teacher-runtime-v11/src" .venv/bin/python scripts/audit_teacher_play.py --run runs/teacher-external-regret-v11-development --policy runs/teacher-external-regret-v11-policy --output runs/teacher-external-regret-v11-play-audit.json
```

Evaluation resume replaces `--output` with `--resume` for that same directory. Original confirmation is still unused and requires independently verified passing development. If V11 development fails, preserve its result and proceed with the reviewed V12 fixed-final candidate; otherwise use the existing V11 confirmation/downstream path. No full teacher qualification, full corpus, Gate 2A certificate or native poker model exists yet. Gates 3–6 remain pending, and the full goal stays active.


## V11 failed; reviewed V12 final policy frozen before development

The original V11 average policy completed all 128 paired deals per original opponent and failed the unchanged full development suite:

| Opponent | BB/hand | Suite-adjusted interval |
| --- | ---: | --- |
| random | +2.576172 | [1.083459, 4.097754] |
| calling-station | +3.568359 | [2.699695, 4.446313] |
| tight-aggressive | +0.314453 | [-0.438977, 1.031775] |
| equity-bucket | -0.515625 | [-1.595703, 0.639673] |

Only random and calling station have positive lower confidence bounds. Complete-journal and statistical recomputation passed, as did all 32 hidden-information probes. Result SHA `30856a567f37c83b4a89adc2f47622ef5ce5a8756c43fbf6d71e67a458749eb8`; evaluation manifest `16efe2b48dd6503c6e3c066fe77d85242643552c74a72f5924707b0ccb411245`; paired journal `8ad199317e7cb8b2b61fabffb260984f8dabbcbdbc3dd7d612b1071fd605ea3e`. V11 remains barred from teaching and did not use full confirmation.

Actual PokerKit replay reproduced all 512 paired records and 2,226 decisions: 105 folds, 575 checks, 236 calls, 309 half-pot raises, 800 pot raises and 201 all-ins. The decision trace is `7680a956e2fb23a089a505a4be9781477acd161a94a435f2e46f5d615a43e1b4`. This is real action variety in a conventional candidate, not a stronger native fly or sufficient poker qualification.

Following the already registered conditional order, reviewed V12 was extracted from that same completed 100,000-step final checkpoint. Its frozen policy SHA is `6b14ca2d9b1dd559fb5de1f8649611a38de488d5eae2f73dc190f97a0367440a`; 306,943 information sets; 86,871,895 disk bytes. All original checkpoint keys, legal masks and regret bytes matched their pinned hashes. A fresh load exactly rederived every action distribution and reverified the complete historical checkpoint/provenance/RNG; no Torch loaded. Final checkpoint metadata SHA `bced70b7cbb4e0b750b5216aa15a960491dbd9d2802c51eca4513371fc2c8725`; regret file SHA `773be6a96db31f28ac1858d14ad5e92003aa96dfe78c1d34d7d20eb1af2d1250`; extraction YAML `a3bd05a204bb8603ffd3e0e5946e3ff27bc2214619c97c2e4832e593c68d356a`; implementation `f6aa18842893bfceb4783ebbac827697cf957c18caf6a230e231491c0d9f1d98`. Source remains reviewed `f70a256a49f9e80274290f1835cb1f9f20d84abb9e573dceff95f4fdb3dd373a` from a97506f. This identity is recorded before V12 development; no policy temperature, checkpoint or action override was selected from the V11 outcome.

After this identity is published, run:

```sh
PYTHONPATH="$PWD/runs/teacher-runtime-v12-reviewed/src" .venv/bin/python -m flyholdem.cli teacher evaluate --policy runs/teacher-final-regret-v12-policy --config runs/teacher-runtime-v12-reviewed/configs/teacher_evaluation.yaml --profile development --output runs/teacher-final-regret-v12-development
PYTHONPATH="$PWD/runs/teacher-runtime-v12-reviewed/src" .venv/bin/python -m flyholdem.cli teacher verify-evaluation --run runs/teacher-final-regret-v12-development --policy runs/teacher-final-regret-v12-policy --allow-development
PYTHONPATH="$PWD/runs/teacher-runtime-v12-reviewed/src" .venv/bin/python runs/teacher-runtime-v12-reviewed/scripts/audit_teacher_play.py --run runs/teacher-final-regret-v12-development --policy runs/teacher-final-regret-v12-policy --output runs/teacher-final-regret-v12-play-audit.json
```

Evaluation resume replaces `--output` with `--resume` for the same directory. Full confirmation remains unused and requires a passing independently verified development result for this exact V12 policy. No qualified full teacher/corpus, full Gate 2A certificate or native poker model exists. The visible fly remains cue-conditioned; Gates 3–6 and the full goal remain pending.


## V12 failed the original full development suite

The fixed final-positive-regret policy `6b14ca2d9b1dd559fb5de1f8649611a38de488d5eae2f73dc190f97a0367440a` completed all 128 paired development deals per original opponent:

| Opponent | BB/hand | Suite-adjusted interval |
| --- | ---: | --- |
| random | +2.021484 | [0.519958, 3.507861] |
| calling-station | +4.255859 | [3.300684, 5.224133] |
| tight-aggressive | +0.072266 | [-0.786157, 0.862805] |
| equity-bucket | -0.273438 | [-1.403870, 0.815979] |

Only random and calling station have positive lower confidence bounds. V12 failed, remains unqualified and did not use full confirmation. All 32 hidden-hole/future-deck/teacher-label probes passed. Complete-journal/statistical recomputation and actual PokerKit replay reproduced all 512 paired records. Result SHA `ed2e619b3c8f66648a9e1ab871c61cf618332095c882719b7b8c2858be292ae5`; manifest `c105b481429c8d801f51dea428925a79fdf26f18e7b3864763277a8f86e6476c`; paired journal `f45c94a6bed725485b88543182f393004adba6330fbd6ae40bcfaa4dcff2e52e`.

The 2,537 actual decisions comprised 132 folds, 798 checks, 343 calls, 526 half-pot raises, 576 pot raises and 162 all-ins. Decision trace `a512b3fd21c1e32243b6c94efe50da098bfebc8e6c131b22bc2552d0684131f9`. These counts show conventional action variety, not sufficient strength or a change to the native fly. Both original V11 and separately registered V12 negative results remain preserved.

The next engineering work implements genuine two-player external-sampling self-play, with a frozen strategy profile across each pair of traversals and opponent-node sampled averaging. Independent exact tiny-game regret/averaging checks and an explicit abstraction-recall review must pass before a new experiment is registered. This is a change from a fixed-population response objective; it does not promise strict positive profits against every scripted opponent or establish finite-run equilibrium. No new self-play horizon, seed range, trained policy or qualification is registered or claimed at this checkpoint. No biological parameter or evaluated opponent is selected from these poker returns.

Full confirmation remains unused. No full-hand teacher corpus, full Gate 2A certificate or native poker model exists. The cue-conditioned visible fly, existing native runtime and environment are unchanged. Gates 3–6 remain pending and the full goal remains active.


## V13 genuine self-play implemented, checked and registered

Implemented a distinct two-player external-sampling regret trainer, numeric average policy, CLI/report workflow and existing evaluator/corpus/play-audit integration. Both public roles use one frozen profile per synchronous pair; regret updates enumerate all legal actions, and sampled opponent nodes accumulate the correct average mass. Complete pairs commit transactionally with exact same-runtime journal-tail recovery. No scripted population, current-strategy extraction, branch pruning, native action override or biological change is introduced.

Independent rational tiny-game tests verify counterfactual regret expectation, own-prefix reach handling, averaging and sampled-opponent memoization. Actual PokerKit branches verify signed BB returns and roles. A constructive abstract-recall review and portable prescribed witness reconstruct 832 current keys and 1,280 earlier own key/action pairs across 112 hands, with 56 button remappings; optimized-mode trace f365bb31ffc4773692d598cb90445c5fa285fa156e48afa0768a4da853068d99. Independent source reviews covered mathematical updates, role/provenance integration, recovery and the exact protocol.

Review found a derived-configuration binding defect inherited from shared hash-string checks: changed recorded method/backend/evaluation contents could retain the old config_hash. The new trainer checks both canonical stored config identity and reconstructed contents in run manifests and checkpoint metadata. Twelve regressions exercise all derived fields, both artifact locations and both checksum-repair cases through resume/export. All 41 trainer integration tests passed. The complete local suite passed 329 tests / two isolated-oracle skips / two existing deprecations in 117.00 seconds. The CLI was actually exercised through interrupted training, resume, final export and truthful two-traversals-per-iteration reporting. Native inference identity remains 1b2c99883afc6e6e30b65e8cf9aafef456953177c10caa443a8d62d531da09ca; its source component remains 0ff3dd35407182b978284b95c4bd4a2256cf092a8686979b79c8fd2e6d00e12f.

V13 is now registered in configs/teacher_self_play_regret_v13.yaml (SHA 3c0992c02167f882a416f0efd8dcaef445baf93719a7988ef1095b9e80a13d66) before training or held-out inspection: 100,000 synchronous iterations / 200,000 complete designated player traversals, 20 BB, unchanged eight buckets/64 samples, combined two-million-information-set capacity and 100,000 nodes per complete pass. Zero initialization; PCG64 sampler seed 92001; two consecutive chance seeds per iteration spanning 1,200,000–1,399,999, disjoint from original held-out/boundary ranges. Fixed final sampled-average export only, with legal-uniform unseen/zero-mass prior. Method, recall construction, primary sources, limitations and exact commands are in docs/SELF_PLAY_TEACHER.md. Self-play gives no finite-run equilibrium, guaranteed script profit or native-learning claim.

After checked publication, freeze committed source/configs/lock/audit scripts and the existing equity binary in runs/teacher-runtime-v13; record and publish the execution ledger. Start `.venv/bin/python -m flyholdem.cli teacher train-self-play-regret --config runs/teacher-runtime-v13/configs/teacher_self_play_regret_v13.yaml --output runs/teacher-self-play-regret-v13 --stop-after 128` with PYTHONPATH pointing to that snapshot's src. Verify all 128 complete pairs and numeric/RNG state by independent reexecution before resuming the same runtime/config with --resume runs/teacher-self-play-regret-v13. No V13 training/model/result exists at this checkpoint. Final policy identity publication must precede the original development suite; only its independently verified pass unlocks unchanged full confirmation. All V1–V12 negative results remain preserved. No qualified full corpus or new native poker model exists; full Gate 2A and Gates 3–6 remain pending. Keep the full goal active.

Prior V12-negative documentation d351b000ab22e7c7b46de7209bbc142bf98a0374 passed CI 34716769300, including fixture/data/oracle and browser checks. Generated data, logs, checkpoints and model files remain ignored and local.


## V13 immutable execution identity, before training

The exact published code from `107fe26ea26a527648f2ac9cec84a6bf6e9f5db2` is frozen in `runs/teacher-runtime-v13`, with source SHA `46a10cd749bfbc0c17c1de7189ed5ec0df8a99ee231199c1969f53be02ed82e5` and complete file ledger `b954aba76e00c5a9688e101718a6f48dae2f0866537d3ba5481650edf9b28edd`. The registered training YAML is `3c0992c02167f882a416f0efd8dcaef445baf93719a7988ef1095b9e80a13d66`; original evaluation YAML `5fb344fad4c27a5a5556764db0d0c43b1f64177ccadafc68b3d81453f52ca9c1`. A separate copy of the existing equity binary is `80846bbc3357982b6310ef05c3c8f9921621c0caa9db1d0c17a1c54cc43207ea`; no binary was rebuilt. Actual fresh-process imports resolved to this snapshot, every ledger file verified, and the reconstructed runtime configuration hash is `1b412c52d09d23de65de6b53cbabc5a7568d4cb8d0b41fcc7e9e55b6d90a1479`. Numeric self-play policy implementation identity is `69b486631ae346f29758b864f4d7211d0814483d4f9ba605eb598764f346f232`. Torch was absent. The repository was clean. No training, policy or held-out play exists at this execution-identity checkpoint.

After this identity is published, use the preceding exact frozen-runtime 128-iteration start command. Independently reexecute all 256 player traversals and compare every checkpoint array and sampling RNG before continuing the same fixed run with --resume. The original final-only export, policy-identity-before-development, independent verification and unused confirmation requirements remain in force. Full Gate 2A and Gates 3–6 remain pending; the native fly is still the cue-conditioned baseline.


## V13 complete resource prefix reproduced; fixed run continuing

The registered first 128 synchronous iterations / 256 player traversals completed in 4.953042 seconds at 65,929,216 bytes peak process RSS. They produced 6,181 information sets (3,074 nonbutton / 3,107 button), 15,651 counterfactual nodes and 6,477 terminal branches, consuming 5,815 sampled-opponent action draws. This is a resource/integrity prefix, not a held-out poker result or separate candidate.

A fresh process independently reexecuted all 256 traversals from the registered initial sampler seed and compared every complete journal record, all numeric array dtypes/shapes/bytes and final RNG state. Complete-chain/private-deal schedule/checkpoint/source/ledger checks passed; partial export was refused before output creation. Training artifacts stayed unchanged and Torch stayed absent. Training manifest `43c746a15086d21711702b24c03d4837e76fbf05dd5dd6e9ce7786538bb811ca`; prefix result `231dc05d403d50822f316f8804077ad0df2bbb101fd273eb470ac8419e35ba15`; prefix journal `fc02d995878ed44559498e81dd85ea6d99d005385e0f0d36e32110f350c03e17`; prefix checkpoint metadata `da0643b97b6956389e8d126d166f28bac5a72ccc360eabc1cd8dff9209e9a1f2`. Prefix journal head `18b2d7f99f39027193b50ad29a8a42924e8bfce4fef4c8cdb04ff583e625937f`. The audit completed in 5.388645 seconds.

The same registered run is now continuing through `runs/teacher-runtime-v13`, source `46a10cd749bfbc0c17c1de7189ed5ec0df8a99ee231199c1969f53be02ed82e5`, using the unchanged --resume command. The original launch manifest records execution commit 9f1dc136ae6224b0205b5d0cf3d1f1117e7e5ccf; implementation code remains 107fe26ea26a527648f2ac9cec84a6bf6e9f5db2. Exact resumed worker PID 83580 was authoritatively observed, exec session 40068; log is the projectless work/mccfr/v13-training-resume.log. Do not start a duplicate or export an intermediate checkpoint. If interrupted, resume the same frozen command and directory. After all 100,000 iterations, export only the fixed final average, independently verify it, publish its identity, then run the original development suite and actual-play reproduction. Full confirmation is still unused and requires its unchanged independently verified development prerequisite.

Implementation milestone 107fe26ea26a527648f2ac9cec84a6bf6e9f5db2 passed CI [34718224670](https://github.com/kosh2shmood/flyholdem/actions/runs/34718224670), including fixture/data/oracle and browser checks. The local complete suite is 329 passed / two isolated-oracle skips in 117.00 seconds. No qualified full teacher/corpus or new native poker model exists. The visible fly remains cue-conditioned; full Gate 2A and Gates 3–6 remain pending, and the full goal is active.


## Exact V13 downstream path preflighted without qualification substitutions

Prepared the separate immutable runs/poker-runtime-v13 from original V13 source `46a10cd749bfbc0c17c1de7189ed5ec0df8a99ee231199c1969f53be02ed82e5` and self-play policy implementation `69b486631ae346f29758b864f4d7211d0814483d4f9ba605eb598764f346f232`, using only the existing native/equity binary copies and ten pinned prerequisite links. No source override, environment change, active-run mutation or native worker. Copied file ledger `cd101db732e3550b6fe835dde10596d6838066a44bc9b591483ab97f42c750c2`; SOURCE record `4a8363016f3bc26ef6b807460e6f74f9d8103e9ab4a4774795fa029e6681b771`. Conditional corpus/Gate 2A/native commands and limits are in docs/DOWNSTREAM_RUNTIME.md. This is preparation only, with no teacher qualification.

Actual preflight passed: four tiny self-play iterations/eight traversals, interrupted and immutable completed resume, exact numeric-average/checkpoint bindings, all 32 boundary probes, and 32 completely reproduced engineering collection hands. Its 161 unqualified targets split 120/24/17 with all streets 72/49/25/15 and both positions 82/79. The real V12 policy loaded through both original and new runtimes; the existing failed 512-record summary matched. All untouched production corpus/qualification/Gate 2A paths refused that failed development evidence before output creation. No qualification stub or fake successful evaluation was used. Positive guarded production corpus export/resume remains conditional on a genuinely confirmed V13 policy; no full graph payload/cue-statistics recheck or teacher-removal execution is claimed here. Every copied and previously pinned prerequisite file remained unchanged.

Freeze helper SHA `93c2b0f1f6c8afb7a8aad23ec1d3fdc2f0209e8dfdd8d69f1da4cdbe5840d6fe`; preflight helper SHA `289c894060b464013135919076264c09250be04e1b9bb4bd88c68c3e41b37b5a`. Both helpers and their detailed JSON results are local projectless work/mccfr files. Native frozen identity remains `1b2c99883afc6e6e30b65e8cf9aafef456953177c10caa443a8d62d531da09ca`. Actual V13 trainer remains PID 83580 / exec session 40068 through its original teacher runtime, last observed past 17,000/100,000 synchronous rounds; automatic complete-pair checkpointing is active (first save at 7,141). Do not duplicate or redirect this worker. Continue the fixed run, then original final-average export, independent verification, identity publication, development and actual-play replay. Full confirmation remains unused and gated. No full corpus/Gate 2A/new native poker model exists; Gates 3–6 and the full goal remain active.

Runtime identity commit 9f1dc136ae6224b0205b5d0cf3d1f1117e7e5ccf passed CI 34718319901; exact-prefix evidence 2d6a490fd55071998953391a8d1f9d593b15785e passed CI 34718558274. Implementation 107fe26 also passed CI 34718224670 and is tagged teacher-self-play-v13-registered. Models, data, logs and generated JSON remain ignored/local.


## V13 final verification prepared; original training still live

The preceding goal turn made concrete progress: the two-player implementation was checked/published, its real resource prefix reproduced exactly, the fixed run resumed, and the separate native downstream runtime passed its actual bounded preflight. On continuation, authoritative ps confirmed the same exact PID 83580 trainer live at 20,100/100,000 rounds, with checkpoint 18,138 and a clean repository. No duplicate, restart or intermediate export was started.

Prepared docs/V13_QUALIFICATION_COMMANDS.md from the actual CLI, original evaluation and unchanged qualification code. It gives exact fixed-final export, development/verification/replay, conditional confirmation and downstream command arguments. --allow-development verifies consistent failures too; it never grants a pass. A failed development or confirmation result must bar dependent teaching and remain preserved.

A read-only final model audit is prepared at projectless work/mccfr/verify-v13-final.py (SHA 1e030f718e0561315a46d9078c4ee52e6baccc33411ae885c0ec21492281fc43). An independent source review found no actionable defect. It requires the exact completed 100,000-round/200,000-traversal result before V13 evaluation exists, invokes original completed_table/load_policy, binds the published source/implementation/training manifest, compares every role key/index and every final action-probability float64 byte against original average masses, and checks all run/policy files unchanged. Training-only street/role visit summaries do not establish full game coverage or strength. Its real early guard was executed against the live partial run and refused it without policy creation or changes to stable training artifacts. The successful final-data branch has not run and must wait for actual completion/export. Do not run it on a live run expecting a final report; use the original frozen PYTHONPATH with -B after the CLI export. Redirect its eventual JSON to work/mccfr/v13-final-identity.json and publish the checked identity before development.

The only actual V13 model work remains the original fixed trainer, PID 83580 / session 40068, source 46a10cd749bfbc0c17c1de7189ed5ec0df8a99ee231199c1969f53be02ed82e5, log work/mccfr/v13-training-resume.log. Full confirmation remains unused. No full qualified teacher/corpus/Gate 2A/new native poker model exists; full Gate 2A and Gates 3–6 remain pending. Keep the full objective active.


## V13 saved checkpoint and full journal prefix verified during training

The prescribed immutable generation `step000000027225-78106503` was captured read-only while the original worker continued. Through the exact original V13 runtime, the independent audit loaded and hash-verified all 15 numeric arrays, restored both role tables, reconstructed the runtime/configuration binding, and verified precisely 27,225 chained journal records / 54,450 player traversals. Checkpoint step, journal head, original PCG64 state and role key counts agreed. The journal's per-role regret-update totals matched restored visits (515,667 / 384,457), and opposite-pass average encounters matched restored average visits (733,559 / 864,248). This is a consistency audit; unlike the initial 128-round audit, it does not reexecute every traversal.

The prefix contains 4,324,008 counterfactual nodes, 1,826,077 terminal branches and 1,597,807 sampled action draws, with 232,997 / 222,558 role keys (455,555 combined). Checkpoint metadata SHA `bf0b4349553eebeaa5b8eb8e9dade416d52463837056465164f23ac11d2048d9`; exact 66,104,278-byte journal prefix SHA `0db5d2d2f373227bc41bff1ddd7417ab63d89000ed7ac91bef7056fb445c9716`; journal head `3379acdb723e442d940871eff42a9ae3a87216916e7405540b02916bfbab98ca`. Two bounded byte comparisons confirmed the original prefix unchanged. Original source/ledger/manifest and copied inputs remained unchanged, Torch was absent, and all temporary copies were removed. Elapsed 18.505207 seconds. Local helper `work/mccfr/audit-v13-checkpoint-27225.py` SHA `44e50bdbecbfc67f07ee08b0a7255e22c338ccbf90fc97f111c597f4c13546f2`; local result `work/mccfr/v13-checkpoint-27225-audit.json` SHA `ea420c1f7c46d753ffdad3790610c2cb16ef1f560bf3184580d53c892399ed36`. No policy export, held-out play, native worker or active training mutation occurred.

Authoritative process observation subsequently confirmed the original PID 83580 / exec session 40068 live, with progress past 38,600/100,000 synchronous rounds. Continue only the existing worker, through original `runs/teacher-runtime-v13`, source `46a10cd749bfbc0c17c1de7189ed5ec0df8a99ee231199c1969f53be02ed82e5`. Resume only after an actual interruption with `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH="$PWD/runs/teacher-runtime-v13/src" .venv/bin/python -B -m flyholdem.cli teacher train-self-play-regret --config runs/teacher-runtime-v13/configs/teacher_self_play_regret_v13.yaml --resume runs/teacher-self-play-regret-v13`. The stale 128-prefix result is historical, not evidence that the live worker stopped. After actual fixed completion, follow docs/V13_QUALIFICATION_COMMANDS.md and the prepared final audit, publishing the checked final identity before the original development suite. No intermediate export or alternate candidate is permitted by this registration.

The read-only native player lifecycle inspection confirms dynamics are reset at hand boundaries, retained between decisions within a hand, and teaching targets are requested only after the neural action is committed. This source observation does not establish useful neural recall or learned poker strategy. The separate curriculum/teacher adapters still need their actual gated execution. Full confirmation remains unused, no full qualified corpus/Gate 2A/new native poker model exists, and full Gate 2A/Gates 3–6 remain pending. Keep the full goal active. The current visible fly remains the cue-conditioned baseline.

Latest prior milestone `10e2bde12ef92bbb8ba9d8f8ba2b91ed94e79033` passed CI `34719708982`; runtime preflight milestone `3011202441c3a04f9bf1226161e9c74bcbc1d79d` passed CI `34719229049`. The checked implementation's complete local suite remains 329 passed / two isolated-oracle skips. This milestone changes only written evidence; generated reports, training data and models remain local and ignored.
